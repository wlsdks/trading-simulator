# Trading Simulator — 제품 기획서 (PRD)

> **한줄요약:** "진짜 돈 걱정 없이 주식을 게임처럼 즐기는 가장 재미있는 가짜 돈 트레이딩 놀이터." 레벨·배지·대회·리더보드로 경쟁하고, 친구와 자랑하고, 시즌마다 새 콘텐츠로 돌아오게 만드는 시장 무관 범용 모의투자(페이퍼 트레이딩) **게임**. (실제 자금·실제 체결이 없는 시뮬레이션.)

> **[성격 고지]** 본 문서는 **모의투자 시뮬레이션 게임의 기획 문서**이며, **실제 투자자문·투자중개가 아니다.** 제품은 실제 자금·실제 체결이 없는 시뮬레이션이며, 표시 시세는 시뮬레이션(또는 향후 지연) 데이터다. 본 문서의 모든 수익화·성과·리더보드 관련 서술은 "모의·엔터테인먼트(재미) 목적"을 전제로 하며, 면책·규제·앱스토어 정책 준수를 항상 고려한다.

---

## 목차

1. [개요·비전](#1-개요비전)
2. [문제·기회](#2-문제기회)
3. [타겟 페르소나](#3-타겟-페르소나)
4. [목표·성공지표 (North Star + KPI)](#4-목표성공지표-north-star--kpi)
5. [스코프 (MVP → v1 → v2)](#5-스코프-mvp--v1--v2)
6. [핵심 기능 상세](#6-핵심-기능-상세)
7. [사용자 경험·주요 화면·플로우](#7-사용자-경험주요-화면플로우)
8. [게임화·소셜·경쟁 (핵심 가치)](#8-게임화소셜경쟁-핵심-가치)
9. [선택적 팁·툴팁](#9-선택적-팁툴팁)
10. [기술 아키텍처·스택 결정과 근거](#10-기술-아키텍처스택-결정과-근거)
11. [데이터·시세 전략](#11-데이터시세-전략)
12. [수익화·비즈니스 모델](#12-수익화비즈니스-모델)
13. [법무·컴플라이언스](#13-법무컴플라이언스)
14. [정책 요약](#14-정책-요약)
15. [유즈케이스 요약](#15-유즈케이스-요약)
16. [리스크·사이드이펙트 대응](#16-리스크사이드이펙트-대응)
17. [로드맵·마일스톤](#17-로드맵마일스톤)
18. [오픈 이슈·의사결정 필요 사항](#18-오픈-이슈의사결정-필요-사항)
19. [비기능 요구·릴리스 기준](#19-비기능-요구릴리스-기준)

관련 문서: [policies.md](./policies.md) · [use-cases.md](./use-cases.md) · [instruments-and-markets.md](./instruments-and-markets.md)(상품·시장·마진/청산/회계 정본) · [social-profiles.md](./social-profiles.md)(소셜·프로필·랭킹 정본) · [game-modes.md](./game-modes.md)(개인/대회 2모드 설계 정본) · [living-market.md](./living-market.md)(살아있는 시장 — 합성참여자·이벤트·속보뉴스·종목랭킹·가상 유니버스 정본) · [monetization-model.md](./monetization-model.md) · [planning-review.md](./planning-review.md)

---

## 1. 개요·비전

### 1.1 비전 한 문장
> **"진짜 돈 걱정 없이 주식을 게임처럼 즐기는 가장 재미있는 가짜 돈 트레이딩 놀이터."**

핵심은 **재미 우선(fun-first)** 이다. 가짜 돈으로 부담 없이 사고팔며, **레벨·배지·대회·리더보드로 경쟁하고 친구와 자랑하고 수집하는** 재미로 매일 돌아오게 만든다. 차별화는 학습·훈육이 아니라 **재미·소셜·수집·시즌 콘텐츠의 완성도**다 — 기존 게임형 앱(Stock Wars류)이 콘텐츠·소셜·시즌 루프가 얕아 1~2주면 자멸하는 지점을, 우리는 깊은 게임 콘텐츠로 반대로 간다. (단, 재미 게임이어도 법적 가드레일 4종은 지킨다 — §1.4.)

### 1.2 현재 상태 진단 (코드 실측 기반)
- **MVP 코드 존재:** Expo(React Native)+TypeScript. 마켓(12종목, 2초 랜덤워크 시세), 매수/매도 바텀시트, 포트폴리오(총자산·평가손익·현금/주식비중·보유종목·거래내역), AsyncStorage 단일 로컬 계정(`ts.account.v1`), 시작자산 $100,000.
- **구조적 공백:** 계정·백엔드·인증 없음. 게임화·소셜 레이어 0. 리텐션 후크 부재. 즉 **"트레이딩 코어만 있고 돌아올 이유가 없는" 상태.**
- **회계·엔진 결함(실측):** 실현손익(realized P&L) 미집계로 전량청산 시 손익 0 표시, 수익률 분모가 유저 간 비교 불가, 시세가 기기-로컬 비결정론·세션 간 휘발, 변동성 파라미터 시간축 오정규화, buy/sell 클로저 레이스로 더블탭 시 현금 음수 가능. 이 결함들이 게임화·소셜·리더보드의 **하드 선행 과제**다.

### 1.3 포지셔닝
모의투자 시장은 (1) 교육형(Investopedia, Wall Street Survivor), (2) 실전 온보딩형(Webull Paper, thinkorswim paperMoney, TradingView Paper), (3) 게임형(Best Brokers, Stock Wars류)으로 나뉜다. 우리는 **순수 (3)게임형**에 포지셔닝하되, 기존 게임형이 못 가진 **깊은 재미·소셜·수집·시즌 콘텐츠**로 차별화하고 (2)의 UX 완성도를 벤치마크한다. **브로커가 아닌 것이 오히려 무기**다 — 실계좌 전환 압박 없이 순수하게 재미에만 집중할 수 있고, 규제 청정 영역(추후 B2B 라이선스 옵션)도 열려 있다.

### 1.4 우리가 하지 않는 것 (Non-goals)
- 실제 체결·실계좌 연동·실전 투자 유도 → **규제상 영구 배제.**
- 개인화된 매수 추천·유료 매수 시그널·리딩 → 한국 자문업/미국 RIA 트리거, **배제**(법적 가드레일 ③).
- 실제 현금 상금 유료 대회 → **배제**(복권법 리스크, 법적 가드레일 ①). 대회는 비현금 보상만.
- 결제·수도(T+1/T+2)·미수금·PDT 등 정산 규칙 → **의도된 단순화(명시적 non-goal)** 로 게임 흐름 단순화.
- 무거운 학습 커리큘럼·학습성과 측정·수료 트랙 → **핵심에서 배제.** 팁은 선택적 툴팁 수준만(§9).
- **1000배 레버리지·인버스·옵션·선물·무기한 등 파생은 전부 가짜돈 게임 메커니즘**이다 → 실자금·실제 체결·현금 상금과 **무관**(가짜 자산·시뮬 가격, 지갑은 절대 음수 불가 — §18 O13·정본 LEV-05). "현실적/정교"는 메커니즘 재현 수준일 뿐, 실제 손실·수익이 아니다.
- **원클릭 카피트레이드·미러트레이딩 미제공** → 타 유저 포지션 복제·"따라 사기" 딥링크/프리필 어떤 티어에서도 배제(자문업·모방 도박화 레드라인, 정본 SOC-06).
- **실시간 고배율 포지션 브로드캐스트 기본 비공개** → 1000배·옵션 그릭스·선물 펀딩 등 고위험 포지션의 실시간 진입/청산을 팔로워 피드로 푸시하지 않음(herding·시세유도 차단, 정본 SOC-06). 공개해도 지연·요약·'추천 아님·모의 기록' 라벨 강제.

---

## 2. 문제·기회

### 2.1 문제 (사용자 관점)
| ID | 문제 | 근거 |
|---|---|---|
| P1 | **진입장벽** — 실전 투자는 무섭고 증권사 앱은 위압적. "부담 없이 지금 눌러보며 재미로 즐길" 안전한 놀이터가 없다. | thinkorswim 온보딩 가파름(반면교사) |
| P2 | **재미의 부재** — 주식은 그 자체로 짜릿하지만, 진짜 돈이 걸리면 즐길 수가 없고, 가짜 돈 앱은 대개 밋밋하다. 오르내림을 마음껏 즐길 안전한 공간이 비어 있다. | 게임형 수요는 크나 만족스러운 제품 부재 |
| P3 | **얕은 게임 루프** — 기존 게임형 모의앱은 대회·리그·수집·시즌 콘텐츠가 얕아 금방 질린다. | 게임형 앱 콘텐츠 빈약(추정) |
| P4 | **리텐션 부재** — 순수 게임 모의앱은 콘텐츠·루프가 약해 1~2주면 이탈. 파산하면 돌아올 이유 없음. | 국내외 앱 리텐션 루프 취약(추정) |

### 2.2 기회 (시장 관점)
- **빈 자리:** 게임형 앱은 많으나 대부분 콘텐츠·소셜·시즌 루프가 얕다. **깊고 재미있는 주식 게임**이라는 자리가 비어 있다.
- **브로커가 아닌 것이 무기:** 실계좌 전환 압박이 없어 순수 재미에만 집중할 수 있다. Webull·토스는 실계좌 전환이 목적이라 이 순수성을 못 가진다.
- **시장 무관 범용성:** 미국주식·크립토·(추후)한국·다자산을 한 시뮬레이터에서. 크립토 24/7 변동성은 게임 리텐션과 궁합이 좋고 데이터 라이선스도 저렴(거래소 API $0).
- **확장 옵션(청정 매출):** 실제 돈 리스크가 없어, 향후 원할 경우 기관·커뮤니티 시트 라이선스로 앱스토어 수수료·자문 규제를 동시에 우회하는 매출선도 열어둘 수 있다.

### 2.3 업계 표준(위생요인)과 차별점
- **위생요인(없으면 감점):** 시작자금 $100,000(업계 표준, 이미 채택 — 좋은 선택), 시장가/지정가/손절 주문, 포트폴리오 손익 추적, 공개/비공개 리더보드.
- **차별점:** ①30초 게스트 온보딩(즉시성), ②레벨·배지·대회·시즌으로 깊은 게임 진행감, ③대회·리그·소셜 자랑 루프, ④수집·시즌 콘텐츠의 깊이.

---

## 3. 타겟 페르소나

### 페르소나 A — "첫걸음 지수" (Primary · 획득 엔진)
- **상황:** 김도현, 26세, 취업 2년차. 유튜브 밈은 알지만 실계좌는 무섭다. FOMO는 있으나 원금 잃기가 두렵다.
- **니즈:** "가입 없이 지금 당장 눌러보며 재미로 즐기고 싶다. 부담 없이 사고팔고 싶다."
- **핵심 기능:** 30초 게스트 온보딩 → 보장된 첫 승리 → 레벨·배지 진행감.
- **성공 신호:** D1 리텐션, 온보딩 완주율.

### 페르소나 B — "리그 등반가" (Retention/Viral 엔진)
- **상황:** 이서준, 22세, 대학생. 게임 랭크에 익숙, 친구들과 경쟁·인증샷을 좋아함. 소액 실투자 중이며 "실력"을 증명하고 싶어함.
- **니즈:** "친구들 이겨서 자랑하고, 매일 들어올 이유(랭크·스트릭)가 필요하다."
- **핵심 기능:** 비공개 그룹 대회, 주간 리그(승급/강등), 스트릭, 공유 카드.
- **성공 신호:** K-factor, 주간 리텐션, 대회 참여율.
- **주의(2차효과):** 절대수익률 단일 순위는 몰빵·도박 프레임을 부추겨 앱스토어 사행성 심사 리스크를 키운다 → **리스크조정·복합 스코어를 순위에 병용**해 게이밍·심사 리스크를 완화한다(재미는 유지하되 법적 가드레일 준수, §8.4).

### 페르소나 C — "그룹 관리자" (B2B/커뮤니티 · 수익 안정 옵션)
- **상황:** 박선영, 41세, 투자 동아리·크리에이터 커뮤니티 운영자(또는 사내 동호회 리더). 멤버 30명에게 단체 대회를 열어주고 싶으나 관리 도구가 없다.
- **니즈:** "그룹 단위로 시작자본·기간·규칙을 설정하고, 참가자 성과·랭킹을 대시보드로 보고 싶다."
- **핵심 기능:** 비공개 그룹 대회 생성(파라미터화), 운영자 대시보드, 그룹 리더보드, 커스텀 규칙.
- **성공 신호:** 유료 시트 수, 갱신율(NRR), 그룹당 활성 참가자 수.

---

## 4. 목표·성공지표 (North Star + KPI)

### 4.1 North Star Metric
> **주간 활성 플레이어 (Weekly Active Players, WAP)**
> = 한 주에 **①거래 세션 1회 이상 OR ②대회·리그 참여 OR ③레벨·배지·수집 진행(1회 이상)** 중 하나라도 한 유저 수 = "이번 주에 재미를 위해 돌아온 사람".

**설계 의도:** 재미 우선 게임에서 성공은 곧 **"얼마나 많은 사람이 즐기러 돌아오는가"** 다. WAP는 거래·경쟁·수집 어느 축으로든 **게임에 참여한 유저**를 세므로, 이 지표가 오르면 재미·리텐션·수익이 함께 정렬된다. 단, 법적 가드레일(사행성 심사)을 위해 **"인당 매매 수"는 별도 안티메트릭**으로 감시한다(§4.2).

### 4.2 지표 위계 — 3계층 (게이트 / 진단 / 안티메트릭)

NSM(WAP)의 위계를 흔들지 않도록 모든 보조 지표를 **3계층**으로 명시 분류한다. 이 위계가 §17 마일스톤·게이트의 판정 기준이 된다.

- **게이트 지표(Gate):** NSM 궤적과 릴리스 통과를 좌우하는 하드 판정 지표. **NSM = WAP 주간 궤적**이 유일한 최상위 게이트이며, 온보딩 게이트(게이트 통과율×첫 매수율)도 여기 속한다.
- **진단 지표(Diagnostic):** 성장·수익·참여를 **진단**하되 그 자체를 목표로 최적화하지 않는 지표 — D1/D7/D30 리텐션, 스트릭 유지, 대회 참여율, K-factor, 전환·RLTV. **리텐션·K-factor·ARPU 등은 전부 '검증 목표(가설)'로 일관 라벨링**하며, 마일스톤에서 하드 게이트가 아니라 추세로 관리한다.
- **안티메트릭(Anti-metric):** 오르면 오히려 경보인 이상감지용 지표 — 인당 일평균 매매 수, 하이롤러 유입률, 절대수익 자랑 노출 급증. 급증 시 게임이 사행/도박 프레임으로 새는 신호로 취급한다(앱스토어 심사 리스크·법적 가드레일). 또한 **개인→대회 페어드 경보**(높은 개인 리텐션 + 낮은 대회 진입 = 개인모드가 종착 이탈싱크라는 신호)를 안티메트릭에 추가한다(§8.5·game-modes §8.2). 모드 퍼널 지표(개인→첫 대회 전환율·첫 대회까지 소요·대회 참가자 중 개인출신 비중)의 상세 정의·목록은 별도 파일 tracking-plan.md에 두고, 본 PRD는 지표명·참조만 유지한다.

> **마일스톤 게이트 분리 원칙:** 각 분기 마일스톤 게이트는 **①WAP 궤적**이라는 상위 게이트와 **②리텐션·바이럴 가설검증**(D7·K-factor 등, 추세·A/B로 검증)으로 **분리**한다. 리텐션 수치는 콘텐츠 물량 검증 전까지 게이트가 아니라 **검증 목표**로 관리한다(§16). 즉 "WAP가 오르는가"가 통과 조건이고, D7/K-factor는 같이 관찰하되 단독으로 릴리스를 막거나 통과시키지 않는다.

### 4.2.1 보조 KPI (계층별, 목표는 검증 대상 가설)

| 계층 | 지표 | 초기 목표(가설) | 근거 |
|---|---|---|---|
| 획득 | 게이트 통과율 × 첫 매수 완료율 | ≥ 70% | 30초 온보딩 차별화 |
| 활성화 | D1 첫 배지·레벨 획득율 | ≥ 60% | 완료자 리텐션 33% vs 20% |
| 리텐션 | D1 / D7 / D30 | 40% / 20% / 8~10% | 캐주얼 게임 벤치마크(가설) |
| 리텐션 | 스트릭 유지(프리즈 포함) | 미사용 대비 +40%↑ | Duolingo 프리즈 +48% |
| 참여 | 주간 대회·리그 참여율 | 추세 상승 | 경쟁 루프 핵심 |
| 참여 | 배지·수집 완성률 / 시즌패스 진행률 | 추세 상승 | 수집·시즌 재미 |
| 참여(모드퍼널) | 개인→첫 대회 전환율 · 첫 대회까지 소요 · 대회 참가자 중 개인출신 비중 | 추세 상승 | 개인→대회 다리 계측(game-modes §8.2, 상세는 tracking-plan.md) |
| 가드레일(모드퍼널) | 페어드 경보: 높은 개인 리텐션 + 낮은 대회 진입 | **이탈싱크 경보(안티메트릭)** | 개인모드=종착 싱크 조기감지 |
| 바이럴 | K-factor(그룹 초대 기준) | ≥ 0.3(v1), ≥ 0.5(v2) | 초대 루프 |
| 수익 | 인스톨→유료 전환 | 2.0~2.6% | RevenueCat 2026 중위 |
| 수익 | Trial→Paid | ≥ 34% | 업계 중위 |
| 수익 | 결제자 연 RLTV | $23~32 | 북미 중위 |
| B2B(옵션) | 유료 시트·그룹 수 | v2 파일럿 3~5개 그룹 | 청정 매출 옵션 |
| 가드레일 | 인당 일평균 매매 수 | **상한 모니터링(안티메트릭)** | 사행성 심사 방지 |
| 가드레일 | 하이롤러 유입률 / 절대수익 자랑 노출 | 추세 감시 | 도박 프레임 조기경보 |

> **원칙:** "매매 수"는 목표가 아니라 **이상감지용 안티-메트릭**이다(법적 가드레일). 급증하면 게임이 사행 루프로 새는 신호로 취급한다. D7 20% 등 리텐션 목표는 게이트가 아니라 콘텐츠 물량 검증 전까지 **검증 목표(가설)**로 관리한다(§16).

---

## 5. 스코프 (MVP → v1 → v2)

> **P1 범위 SSOT:** Phase 0 / MVP / P1-deferred의 실행 범위는 [p1-scope.md](./p1-scope.md)를 단일 출처로 삼는다.

각 단계에 **들어가는 것 / 빠지는 것 / 빠지는 이유**를 명시한다.

### 5.0 상품·시장 확장 스코프 (IP1~IP4) — 오너 확정 신규 스코프

오너가 스코프를 확장했다: **레버리지 롱/인버스 1000배·옵션(블랙숄즈)·선물·무기한·ETF/ETN·코인·KR/US/JP 3시장·소셜(프로필/랭킹).** 현실성 수준은 "메커니즘은 진짜처럼(정교), 손실은 가짜 돈처럼"이다. 상세 계약명세·공식·엔진·회계는 [instruments-and-markets.md](./instruments-and-markets.md)(상품·시장·마진/청산/회계 정본)와 [social-profiles.md](./social-profiles.md)(소셜 정본)를 참조하며, 본 PRD는 스코프·게이트·리스크만 요약한다. 아래 **IP1~IP4**는 상품·시장·소셜 확장의 제품 phasing이며, build-ready P1 실행 범위는 [p1-scope.md](./p1-scope.md)가 우선한다. Instrument phasing(IP1~IP4)은 어떤 상품·시장을 어떤 순서로 출시할지의 축이고, build slice(Phase 0 / MVP, [p1-scope.md](./p1-scope.md))는 엔지니어링이 먼저 구현할 범위의 축이므로 둘을 혼동하지 않는다. 스키마는 IP1에서 예약(ACC-17 마이그레이션 폭발 방지)한다.

| 단계 | 상품·시장 | 엔진·회계 | 소셜 |
|---|---|---|---|
| **IP1** | 미국장 **현물 롱/숏**(ORD-25 로케이트·리콜·buy-in) + **레버리지 롱/인버스 1000배**(격리마진·강제청산·부분청산) + **현물 ETF** | LEV-01~08(마진·청산·손실상한·보험기금·마진이자·하이롤러 리그), ACT-15·ACC-21·ACC-22·ACC-23·GRF-16 필수. ADL(LEV-06)·교차마진은 골격만 | 프로필·공개범위·비대칭 팔로우·성과표시·herding 방어·랭킹/프로필(SOC-01·02·03·05·06·07·08 + SOC-04 레버리지/롱숏 리그 분리) |
| **IP2** | **옵션**(콜/풋·유럽식 CASH_SETTLE 기본·블랙숄즈) + **선물**(CME ES/MES/MNQ) + **무기한**(펀딩) + **레버리지/인버스 ETF·ETN** | MKT-15(옵션 프라이싱·그릭스·IV)·MKT-16(무기한)·MKT-17(선물)·MKT-18(레버리지 ETF·ETN)·ORD-24(옵션 주문/행사/배정)·ACC-20(옵션 회계). LEV-06 ADL·교차마진 활성 | SOC-04 옵션/선물·무기한 리그 축 개방 |
| **IP3** | **한국·일본장**(KRX KOSPI200·KRW / OSE Nikkei225·JPY) + **FX 멀티월렛** | MKT-19(멀티거래소·DST·반일장)·MKT-20(세션 상태머신·동시호가·갭)·MKT-21(±30% 상하한가·VI·値幅制限·LULD)·MKT-22(FX 24/5)·ORD-26(환전·auto-FX)·ACC-24(멀티월렛·기준통화 정규화·FX_ADJ) | SOC-09 크로스마켓·다통화 프로필 |
| **IP4** | **코인**(24/7 현물 + 무기한) | CRY-01~06(24/7·3층가격·소수점·펀딩·24/7 리더보드·김치프리미엄). 무기한 엔진은 IP2 그대로 재사용 | SOC-04 코인 리그 축 개방 |

> **회계 2대 엔진 경계(정본 §0.1):** (A)costBasis 엔진 = 현물·ETF·ETN·롱옵션·FX현물(cash≥0 유지, CASH 계좌 가능), (B)margin-MTM 엔진 = 레버리지·인버스·현물숏·선물·무기한·숏옵션(cash≥0 면제·equity≥0, MARGIN 전용). **레버리지/인버스 ETF는 ±3배 상품(A군, 마진·청산 없음)**, 오너의 1000배는 **계좌레버리지(B군)** — 이 경계가 "레버리지=cash≥0 붕괴" 오해를 원천 차단한다.

### MVP (Q3 2026) — "혼자서도 돌아온다"
현재 코드는 트레이딩 코어만 있고 돌아올 이유가 없다. MVP 목표는 "혼자서도 3일은 돌아오게".

**IN:**
- (기존) 12종목 시뮬 마켓, 매수/매도, 포트폴리오, 거래내역, $100k — 유지.
- **[신규] 개인 모드 = 홈·마켓·자산의 기본 상태**(별도 진입 화면 아님, 대회 미참가 = 개인 모드): **무한 리셋 + 상한 리필**(ACT-11 가드레일). 마케팅·UI 카피에서 **"무한 리필/무한 자금" 표현 전면 금지**(앱스토어 시뮬레이션 도박 조항 회피). 상시 모드 배지(§7.6), 독립 진행 루프, **리셋에 지워지지 않는 개인 평생 실력지표**(TWR/리스크조정 복합스코어 히스토리, §8.3.1). 상세 game-modes §2.
- **[신규] 콜드스타트 방어형 대회 프리미티브 1개**(로스터 독립형 고스트/벤치마크, 결정론·비동기, 시스템 고스트봇 상대 — '**시스템 상대**' 명시): 유저풀 없이도 솔로 유저가 1일차에 완결 스테이크·리더보드를 경험. 대회 후크를 P1로 미루지 않고 MVP에서 조기 계측. 상세 game-modes §4.6·§8.1.
- **Phase 0 회계 하드닝(버그성, UX 변화 0):** 실현손익 원장, 정수 센트 회계, 외부현금흐름(externalFlows) 분리, `schemaVersion`+마이그레이션 골격, UUID 거래 ID, buy/sell 원자화(리듀서), priceEngine seed 주입(결정론).
- **시세 엔진 교정:** 결정론 순수함수화, 로그정규 GBM + 연환산 변동성 정규화 + 소폭 양(+)의 드리프트, 시세 영속성/재계산.
- **면책·연령게이트·개인정보:** 온보딩 미세 배너 + (결제/소셜 진입 직전) 전면 동의. 연령 수집은 **출생 연월**(일 미수집).
- **30초 게스트 온보딩 + D1 보장된 작은 승리:** 실패 불가능 가이드 매수 1건 + '플레이 시작' 배지.
- **게임 콘텐츠 진행감 후크:** 레벨 진행(언락) 트랙, 미션/챌린지 v0, 선택적 팁 카드 24장(툴팁), 로컬 스트릭(+프리즈, 랭킹 미연동), 배지 컬렉션.
- **지정가·손절 주문(게임 플레이 심화 기능).**

**OUT (이유):**
- 서버/인증/클라우드 동기화 — MVP 로컬 유지(라이선스 0, 규제 최소, 속도↑). 단 데이터 모델은 서버 이관 가능하게 설계.
- **로컬 리더보드 노출** — 순위 기대 형성 후 서버 전환 시 재계산으로 신뢰 붕괴 → "개인 연습 기록"으로만 프레이밍, 순위 미노출.
- 실시간 실데이터 — 라이선스·비용·재배포 리스크. 시뮬 유지가 안전.
- 수익화(구독/광고) — 볼륨 없이 무의미.

> **[정합] MVP 검증 리스크(game-modes §8.1):** 재미·리텐션의 무게중심은 대회에 있으므로 **개인 단독 D7은 '약한 프록시'**다(§4.2에서 D7을 이미 검증 목표 가설로 강등 — 유지). 위 콜드스타트 방어형 대회 프리미티브를 MVP로 당겨 대회 후크 자체를 조기 계측하고, 재미 명제 전체를 P1로 미루지 않는다.

### v1 (Q4 2026 ~ Q1 2027) — "함께라서 돌아온다"

**IN:**
- **계정/인증 + 서버 백엔드:** 소셜·리더보드의 전제. 게스트→지연가입(소셜/대회 진입 시점에만).
- **다계좌 구조 리팩터 + `useEntitlement()` 게이팅 계층.**
- **커뮤니티 모더레이션(소셜 v1의 하드 선행 게이트):** 신고·차단·모더레이션 큐를 소셜(공유 카드·활동 피드·그룹 대회·친구) **공개 전 선행 요건**으로 편입(LEG-06 정합). 어떤 UGC/소셜 노출 기능도 신고·차단·모더레이션 파이프라인 없이는 켜지 않는다.
- **그룹/길드 비공개 대회(최우선 소셜 기능):** 시작자본·기간·규칙 파라미터화. 서버 시드 결정론 시세. 친구 초대 = K-factor 엔진.
- **주간 리그(약 30명 풀, 승급/강등) + 리스크조정 순위:** 절대수익률 단일 순위 금지(사행성 심사 리스크). MDD·분산점수·규칙준수율 병기. 초보/숙련·쉬운/리얼 리그 분리.
- **챌린지·시즌 콘텐츠 확장:** 챌린지·미션 완료 시 대회 보너스 크레딧·코스메틱.
- **수익화 v1:** Plus 구독(월 $6.99 / 연 $41.88, 7일 체험), 리워드 광고(상한·쿨다운), 코스메틱 IAP.
- **크립토 자산군(거래소 API):** 단, 재배포 약관 개별 법무검토 통과 후에만 실데이터. 미통과 시 시뮬 유지.
- **리얼/쉬운 모드 분리(모드별 리그 분리).**

**OUT (이유):**
- 실제 현금 상금 대회 — 복권법. 비현금 보상만.
- **현금 상금·유료 참가비 하이롤러 리그** — 앱스토어 사행성 심사 리스크. 유저풀·평판 안정 후 조건부(§16). (단, **레버리지·파생 성과의 리그 격리 자체는 IP1부터** — LEV-08·GRF-16에 따라 고위험 리그로 분리 집계하며, 이는 격리 재원이지 현금 상금이 아니다.)
- 한국 실시간 KRX — 라이선스 비용. 지연/일봉으로(시장 자체는 IP3).
- B2B 콘솔 — v2.

### v2 (Q2 2027~) — "제도·규모로 확장"

**IN:**
- **B2B/커뮤니티 콘솔(옵션):** 그룹 대회·대시보드·커스텀 규칙, 연간 시트 라이선스, 라이트/고대비 테마.
- **미국주식 실(지연)데이터 승격:** Provider 추상화 위에 재배포 허용 유료 플랜. "15분 지연" 배지 명시.
- **파생·다자산 상품 승격(§5.0 IP1~IP4):** 공매도·레버리지 1000배·격리마진/강제청산은 상품 스코프상 **IP1**(정본 LEV 도메인), 옵션·선물·무기한·레버리지ETF/ETN은 **IP2**, 3시장·FX는 **IP3**, 코인은 **IP4**로 배치. 원금초과 손실 고지 세트·고위험 리그 격리는 LEV-05/LEV-08·GRF-16으로 정본화(O13 해결, §18). 캘린더 로드맵 정합은 §17 참조.
- **코퍼레이트 액션:** 배당·액면분할 이벤트 스케줄러.
- **상위 프리미엄 티어(Pro/Max), AI 도우미(선택적 팁·요약; 개인 맞춤 매수 추천 금지 — 법적 가드레일 ③).**

**OUT/조건부:**
- 유료 대회 — AMOE·실력콘테스트·지오펜싱·세무 갖춰질 때만, 법률검토 후.
- 하이롤러 리그 — 연령차단·비현금·강한 면책·심사노트 첨부 조건부.

---

## 6. 핵심 기능 상세

### 6.1 회계·트레이딩 코어 (Phase 0 — 최우선)
게임화·소셜·리더보드의 **하드 선행 과제**. 신규 UX 변화 없이 정확성만 확보한다.

| 결함 | 처방 |
|---|---|
| 실현손익 미집계(전량청산 시 손익 0) | `realizedPL` 원장 신설: 매도 시 `(fillPrice−avgCost)×qty − sellFees` 누적, 계좌·종목·체결 3층 기록 |
| 수익률 분모 유저 간 비교 불가 | (A)리더보드용 = 시작자본 $100k 고정 분모 + 외부흐름 차감, (B)실력 = TWR 이원화 |
| 외부현금흐름 오염 | 광고·미션 크레딧을 `externalFlows` 원장에 분리, cash 직접 가산 금지 |
| float 누적오차 | 정수 센트(USD=cent, crypto=sat) 저장, 표시 직전 변환, **round-half-even 단일 규칙** |
| buy/sell 레이스(더블탭) | 가드를 함수형 업데이터/리듀서 내부에서 원자 평가 + UUID+멱등키 |
| 시세 비결정론·휘발·과변동 | `price=f(seed,ticker,tickIndex)` 순수함수, 로그정규 GBM, 연환산 변동성 정규화, 소폭 드리프트, 영속/재계산 |
| 단일 계좌유형 가정(마진계좌 부재) | **ACT-15 계좌유형 분기(CASH/MARGIN)** — 계좌에 `tradingType(CASH\|MARGIN)` 부여, 불변식 세트를 유형별 분기 실행. 스키마·분기 골격을 **Phase 0 필수**로 예약(정본 §7.2) |
| 자기자본 항등식이 현물 전용(`equity=cash+Σmark×qty`) | **ACC-21 통합 자기자본·3버킷 항등식** — `equity = cash + Σspot(mark×qty·부호) + Σderiv((mark−avgEntry)×qty×mult) + ΣoptLong − ΣoptShort`, 3버킷 현금관리(`reservedMargin`·`freeCash`)를 **Phase 0 필수** 골격으로 예약(정본 §7.1) |

> **계좌유형 불변식 분기 (Phase 0 하드 요건, 정본 §7.2 ACT-15):** 현행 `cash ≥ 0`(I1)은 **CASH 계좌에서만 유지**된다. MARGIN 계좌에선 I1을 폐기하고 **`walletBalance ≥ 0`(IM1) + `reservedMargin ≤ cash`(I1a) + 포지션별 `isolatedMargin ≥ 0`(IM2) + `equity ≥ 0`(IM4, 청산 후 보험흡수 clamp)** 로 분기된다. 이 분기가 없으면 레버리지/파생(IP1)에서 회계가 붕괴하므로, 계좌유형 필드·3버킷 필드·분기 골격을 Phase 0에서 스키마 예약한다(로직 배선은 IP1). 상세 공식·불변식(IM1~IM6)은 정본 §7 참조.

### 6.2 주문·체결 시스템
- **주문유형 축과 마찰 축 분리(중요 정합):** LIMIT/STOP/TAKE_PROFIT은 **쉬운 모드·무료**로 고정. 프리미엄/리얼모드 게이트는 **OCO/BRACKET/TRAILING·마찰(수수료·슬리피지)·대규모 대회개설**로 한정. (MON-01 매트릭스의 '지정가' 표기를 'OCO/고급 조건부 주문'으로 정정.)
- **도입 순서:** MARKET+LIMIT → STOP/TAKE_PROFIT → STOP_LIMIT/OCO/BRACKET/TRAILING.
- **대기주문 엔진:** 틱 갱신 직후 `evaluatePendingOrders`(순수함수, 결정론 우선순위). 틱 스냅 기반 판정(갭 체결은 현실 반영).
- **체결가:** 쉬운 모드 = mid 무마찰. 리얼 모드 = bid/ask 스프레드(변동성 연동) + 제곱근 시장충격 슬리피지. **대회 슬리피지는 본인 주문 notional만의 순수함수(개인 독립 유동성 풀, 공유 오더북 없음)** 로 결정론·감사 성립.
- **수수료·거래세:** 모드/시장별 파라미터. 매수수수료 자본화, 매도수수료·세 실현손익 차감. 롱 레버리지 **margin interest**도 숏 대차수수료와 대칭으로 절대시각 기준 일별 차감.
- **주문 확인 스텝:** 소액·시장가 1스텝, 고액·고급주문 2스텝(적응형 마찰).
- **신규 스코프 주문축 확장(정본 참조):**
  - **옵션 4방향·계약선택(ORD-24, IP2):** BUY_TO_OPEN/SELL_TO_CLOSE(롱)·SELL_TO_OPEN/BUY_TO_CLOSE(숏) 4방향 + 계약선택(`optionType·strike·expiry·exerciseStyle·settleType`). 게임 MVP 기본 = 유럽식·CASH_SETTLE, 만기·자동행사·배정 라이프사이클.
  - **선물 롤오버(ORD/MKT-17, IP2):** 사용자개시 캘린더스프레드(근월 청산+차월 개설 원자체결), 만기 N일전 알림, 미롤 시 자동 현금정산, 백조정 연속월물 차트.
  - **FX 환전·auto-FX(ORD-26, IP3):** `FxOrder{fromCcy,toCcy,amountFrom}` 명시 환전 + 외화종목 매수 시 부족분만 원자 환전하는 auto-FX(쉬운 모드 ON·리얼 모드 OFF). 월렛별 `cash≥0` 가드.
  - **공매도 로케이트 게이트(ORD-25, IP1):** 진입 전 `availableToBorrow` 확인 → 부족 시 `REJECT(NO_LOCATE)`. HTB 등급·대차수수료(CARRY)·리콜/강제환매(buy-in).
  - **무기한 마크가 기준 강제(MKT-16, IP2):** 미실현·VM·펀딩·**청산 판정은 last가 아니라 인덱스앵커 mark**(median-guard)로 계산(단일틱 wick 부당청산 방어). 체결만 last.
- **강제청산·부분청산(LEV-04, IP1):** `marginLevel ≤ 1` 발동 → 시장가 청산주문. **부분청산이 표준 경로**(targetLevel 회복 최소수량만 단계 축소, 실패 시 전량 에스컬레이션). 청산은 **서버 권위·결정론·멱등키·원자커밋**(MKT-12·ACC-09). 각 Fill은 개별 원자 트랜잭션 + 실현손익·청산수수료 스탬프. 상세 청산가·보험기금·손실상한은 정본 §4·§18 O13.

### 6.3 포트폴리오·성과
- 총자산 = cash + Σ(mark×qty). **리더보드용 mark는 단일 정의(last)로 고정**(형평성). 미실현/총손익 = realized + unrealized.
- 위험조정 지표: MDD, 분산점수(1−HHI), 승률, 샤프유사(표본 부족 시 N/A). 자산곡선 스냅샷은 **절대 캘린더 시각** 기준(앱-오픈 의존 금지).

### 6.4 코퍼레이트 액션 (v2)
- 분할: `qty*=r`, costBasis 불변(avgCost는 `costBasis/qty` 파생) — **avgCost 스칼라 저장·조정 폐기**. 배당: **투자소득(endogenous)으로 총수익률 numerator에 포함**하되 광고·미션 크레딧만 external 차감(배당 이중 페널티 회피). 상폐: 마지막 가격 강제청산.

---

## 7. 사용자 경험·주요 화면·플로우

### 7.1 정보구조(IA) — 5탭 + 점진적 공개
```
[홈] [마켓] [챌린지] [랭킹] [내 자산]
 🏠   📈    🎯     🏆    💼
```
- **온보딩 직후:** 홈·마켓·자산 3탭만 노출. 챌린지·랭킹은 첫 미션 완료/서버 활성 이후 점진 공개(초보 위압감 최소화). 감춘 탭은 "준비 중" 빈상태를 명확히 설계.
- **[마켓] 탭 = 마켓 펄스(Market Pulse) 디스커버리 (6번째 탭 금지, living-market.md §4):** 종목 랭킹은 신규(6번째) 탭이 아니라 **[마켓] 탭 내 '마켓 펄스' 디스커버리 표면**(UC-A03 확장)으로 배치한다. 거래대금·거래량·등락률·체결강도·RVOL·시가총액·인기의 **다차원 종목 랭킹**을 렌즈(Lens) 칩 + 펄스 카드로 탐색한다. **[랭킹] 탭의 유저 리더보드와는 물리·데이터 격리**(종목 랭킹 ≠ 유저 랭킹): 종목 인기·거래량은 유저 점수(GRF-01)에 절대 유입 금지, 종목 데이터는 표시용(비채점)·시뮬 결정론이고 유저 리더보드는 실현손익 원장·서버 권위·채점 하드다. 모든 마켓 펄스 표면에 '시뮬 데이터·시뮬레이션 참여자·추천 아님' 라벨 상시(§13.4). 상세는 living-market.md §4 참조.
- **챌린지 탭 역할:** 미션·챌린지·배지·시즌·수집 진행 **허브**. 선택적 팁/툴팁은 매매·포트폴리오 화면 **인라인(맥락형)**으로만 노출하고 별도 탭으로 격리하지 않는다.
- **[랭킹] 탭 = 대회 디스커버리 (6번째 탭 금지, game-modes §4.1):** 대회 둘러보기는 신규(6번째) 탭이 아니라 **[랭킹] 탭 상단 세그먼트 `[리그 | 대회 | 리더보드]`**로 배치한다. '**대회**' 세그먼트는 **첫 미션 완료·서버 활성 이후 노출**(§7.1 점진공개 정합). 개인 모드는 탭이 아니라 '어떤 계좌로 거래 중인가'의 상태이며 상시 모드 배지로 표현한다(§7.6).
- **기술 골격:** `useState` 탭 스위칭 → **Expo Router**(딥링크·웹 URL·뒤로가기 — 소셜 바이럴의 load-bearing 요소).

### 7.2 온보딩 — "30초 첫 매수 + 입성 / 플레이어 탄생"
1. 스플래시·가치제안 + 하단 미세 면책 + 앱스토어 연령등급 고지.
2. **게스트 즉시 진입** → 코치마크가 종목 지목, 잔고 충분한 수량 프리필.
3. 원탭 매수(튜토리얼 태그) → **정체성 보상 '입성 / 플레이어 탄생' 배지 획득 연출** + 다음 액션 넛지. 체결·손익 자체에는 컨페티를 붙이지 않는다.
4. 홈 착지(오늘의 미션 1개 + 자산 스냅샷만; 위젯은 진도 연동 점진 노출).

**핵심 원칙(정합 확정):**
- **면책 시점(단일화):** 온보딩 시에는 **미세 배너만** 노출한다. **전면 면책 동의(체크박스+타임스탬프+버전 기록)** 는 **소셜·결제·대회 진입 직전에 트리거**한다. 순수 로컬 게스트(비수집·오프라인) 경험에는 미세 배너만 적용하고 전면 게이트를 걸지 않는다. "30초"는 이 순수 로컬·비수집 경험에만 적용된다.
- **연령 수집 정밀도(데이터 최소화):** 연령 입력은 **출생 연/월만** 받는다(일·정확한 생일은 미수집). 아동 식별정보 축적을 피하고 연령게이트(한14/EU16/미13, §13.3) 판정에 필요한 최소치만 보관한다.
- **첫 배지 획득을 명확히 축하**해 '시작의 쾌감'을 준다. 이 축하는 매수 체결이나 P&L이 아니라 **정체성 진입(입성 / 플레이어 탄생)** 에 귀속한다. 프리필 매수가 잔고 초과 시 자동 하향 조정(실패 불가 보장).
- (선택) 직후 "이 종목은 왜 이렇게 움직일까?" 같은 가벼운 팁 카드를 넛지로 제공할 수 있다(강제 아님, 스킵 가능).

### 7.3 첫 매매(정식) — 리서치→주문→확인
```
마켓 리스트 → [종목 상세: 차트·통계·내 보유·하단 고정 매수/매도] → [매매 시트]
 → 주문유형(시장가 기본 / 지정가·손절 고급) → 수량(±·MAX·금액역산)
 → [주문 확인: 예상가·수수료·잔고영향] → 비축하 토스트 → 상세 복귀
```

### 7.4 일일 복귀 — 홈
스트릭·오늘의 미션 2~3개·자산 스냅샷·(서버 후)리그 미니. **가드레일:** 데일리 미션 2~3개 상한, 푸시 1일 1~2회 상한, "연속 손실 시 잠깐 쉬기" 넛지(FCA '잦은 푸시' 안티패턴 회피).

**이벤트·속보 알림 웰빙(살아있는 시장, living-market.md §3.6·§7):** 이벤트·속보 뉴스가 추가돼도 알림은 위 가드레일 하위에 있다.
- **단일 상한 통합:** 이벤트/속보 푸시는 **일반 푸시와 합산해 1일 1~2회 단일 상한** + 조용시간(22–08) 다이제스트 1건. 상한 완화는 안티메트릭 영향 A/B 승인 없이 불가한 **변경 게이트로 봉인**(느슨한 쪽 수렴 유인 차단).
- **손실 포지션 악재 푸시 억제:** 손실 중인 보유 종목의 악재 속보는 **즉시 푸시를 억제하고 인앱 지연 노출**로 전환하며, 착지 화면에 매수/매도 CTA 없이 **본인 손절선 리마인더만** 표시(loss-chasing 방어).
- **연속 손실 톤다운:** 연속 손실 유저는 이벤트 푸시 자체를 톤다운한다.
- **알림 착지 = 서사 화면:** 알림 탭 시 착지는 **서사(뉴스 헤드라인+차트)** 이며, **매수 시트 프리필 금지**(반사적 원탭 추격매수 유인 제거). 팡파레·축하 이모지 금지.

### 7.5 빈/로딩/에러·디자인 시스템·접근성
- **로딩=스켈레톤**, **빈 상태 4요소**(아이콘+원인+다음행동+CTA), **에러=한국어 3원칙**(무슨일/왜/어떻게)로 현행 영문·무맥락('Not enough cash') 전면 교체.
- **등락 삼중 인코딩**(색+화살표▲▼+부호) → WCAG 1.4.1. 금액은 등폭(tabular-nums). `theme.ts`를 시맨틱 토큰으로 승격.
- **접근성 정합:** 라이브 리전은 **액션 결과(체결·에러)에만 assertive**, 2초 시세 갱신은 `aria-live=off`+온디맨드 announce(스크린리더 announcement 폭탄 방지). 배지 중첩(지연·모드·면책·출처) 시 시각 위계 붕괴 방지 위해 배지 예산 관리.
- **라이트/고대비 테마:** v1 다크 고정 가능하나 시맨틱 추상화 선반영, B2B 파일럿 전 라이트/고대비 최소 1종 준비.
- **현지화(로케일 우선순위 단일 참조):** 제품 UI 언어는 **①기기 로케일 → ②출시 시장 기준 → ③`en` 최종 폴백** 순으로 결정한다(미국 소프트런치 = 사실상 영어 우선, 기기 로케일이 지원 언어면 자동 선택). **"한국어 우선"은 내부 산출물 문서 작성 규칙일 뿐 제품 UI 언어가 아니며**, PRD·정책·유즈케이스 전 콘텐츠의 로케일 우선순위는 **본 §7.5를 단일 참조원(single source)** 으로 삼는다.

### 7.6 모드 배지·계좌 스위처 (개인/대회) — 정본 game-modes §2.1·§4.1·§7.4

- **상시 모드 배지:** 홈·마켓·자산·**매매 시트** 상단에 현재 거래 컨텍스트를 항상 표기하는 모드 배지(예: `개인` / `대회명·D-3`)를 상시 노출한다. 같은 종목이라도 계좌 컨텍스트에 따라 시세·규칙이 다르므로(§11.1 계좌별 활성 시세 분리) 배지는 오인 방지의 하드 요건이며, 배지 예산(§7.5)으로 시각 위계를 관리한다.
- **개인 모드 = 별도 화면 아님:** 개인 모드는 진입 화면이 아니라 **홈·마켓·자산의 기본 상태**다(대회 미참가 = 개인 모드).
- **계좌 스위처 규칙:** 개인·대회 잔액 **합산 표시 금지**, 계좌별 분리 표기 + 모드 배지('내 돈' 오해·이전 착시 차단, ACT-13 엣지). **대회 계좌는 리필 버튼 미노출**(ACT-11). **아카이브·프리미엄 만료 초과 계좌는 읽기전용 잠금.**

---

## 8. 게임화·소셜·경쟁 (핵심 가치)

> **이 섹션이 제품의 심장이다.** 트레이딩 코어는 게임판이고, 진짜 제품은 **레벨·배지·대회·리더보드·시즌으로 경쟁하고 자랑하고 수집하는 재미**다. 재미·경쟁·소셜·수집을 최우선 가치로 설계한다.

### 8.1 제1원칙 — 재미·경쟁·수집을 보상한다
스트릭·XP·배지·레벨·리그의 보상 대상은 **"게임을 즐기고, 경쟁하고, 진행하고, 수집하는 것"** 이다. 매일 돌아올 이유, 친구를 이길 이유, 컬렉션을 채울 이유를 겹겹이 쌓는다.

- **단, 법적 가드레일은 지킨다(재미 게임이어도):** ①실제 현금 상금 대회 없음, ②"모의투자 시뮬레이션" 면책 상시, ③개인 맞춤 매수 추천 없음, ④실데이터 라이선스 확인. 또한 앱스토어 **사행성(도박) 심사 리스크**를 피하려고 순위는 절대수익 단일이 아니라 복합 스코어를 병용한다(§8.4). 이는 재미를 죽이는 규칙이 아니라 **스토어에서 살아남기 위한 최소 방어선**일 뿐이다.
- **제1원칙 연장 — "짜릿하되 추격은 손해로 끝난다"(living-market.md §7):** 살아있는 시장(합성 참여자·이벤트·속보 뉴스)이 주는 짜릿함의 **총량은 유지하되, 추격만 구조적으로 손해**가 되게 설계한다. 이는 사후 통계·비차단 배너가 아니라 **엔진 물리·연출·마찰의 구조적 가드**로 강제한다: ①이벤트 3막 라이프사이클의 **여진 되돌림(ρ=0.3~0.5)** 이 급등 추격의 기대수익을 음(−)으로 만들고, ②**루머 홀딩 기대수익 EV ≤ 0**을 CI property test 하드 게이트로 강제(승률뿐 아니라 페이오프까지 포함)하며, ③여진 구간에서 급증 배지·불꽃 게이지를 **실시간 시각 냉각**(페이드·색전환)해 "몰림→되돌림"을 같은 화면 연출로 학습화한다. 짜릿함을 죽이지 않으면서 추격 유인만 물리적으로 거세하는 것이 재미의 구조적 가드다. 상세는 living-market.md §7 참조.

### 8.2 3종 화폐 분리
| 화폐 | 획득 | 용도 | 하드 가드레일 |
|---|---|---|---|
| **XP** | 판단 품질·시나리오 완주·신규 가상회사 첫 거래·손절 준수·수집·배지·대회 등 **P&L 독립 진행 행동** | 레벨·개인 코스메틱·상품 언락(검증 진행만) | **거래량 XP=0**. P&L·매매 횟수·손익 실현으로 XP 지급 금지 |
| **가상현금 $** | 초기 $100k, 미션/대회 보너스 | 매매 원금 | 손실 무게감 유지(게임 긴장감) |
| **코인/젬** | 리워드 광고·IAP·업적 | 코스메틱·편의 | **코인으로 가상현금 구매 불가**(현금상금·사행성 우회 방지) |

### 8.3 스트릭·배지·미션
- **스트릭:** "매일 돌아오기"를 보상 — 로그인 + 아무 게임 액션(거래·미션·대회 확인) 1개면 유지. 프리즈 필수(무료 주1회+미션 보상; **직접 유료판매 배제** — 손실회피 수익화 다크패턴 회피). 위켄드 앰네스티, 장기 미접속자 압박 알림 중단(웰빙 가드).
- **배지:** 수집·탐험·경쟁·마일스톤·시즌 등 **폭넓은 컬렉션**으로 "채우는 재미"를 극대화. 단 노골적 도박 조장 배지(몰빵 100%·하루 20거래·**속보 즉시매수** 등 추격·과매매 유도형)는 **카탈로그에서 원천 배제**(사행성 심사 방어, living-market.md §7-8).
- **축하 연출 SSOT:** 컨페티·팡파레·대형 축하 연출은 **실현/미실현 P&L과 분리**하고, P&L 독립 채널에서만 발화한다: 순자산 마일스톤(외부유입 차감 후), 개인 평생 실력지표 갱신, 좋은 판단 스트릭, 배지/레벨 획득, 도감·세트 완성, 시즌/시나리오 완주, 공식 대회 티어·승급. 매수/매도 체결·수익 실현·손실 회복 자체에는 컨페티를 붙이지 않는다. 단 "손실 후 즉시 재매수 유도" 같은 loss-chasing 유발 연출은 배제(웰빙·심사 가드).
- **데일리 미션 다양성:** 미션을 "많이 사기"류로 단조롭게 반복하면 지루하고 사행 프레임도 커짐 → "새 종목 탐험·대회 참여·컬렉션 채우기·챌린지 클리어" 등 **다양한 재미 축**으로 구성.
- **[신규] 비추격 판단 보상 미션 축(living-market.md §7-8):** 미션은 **소비량이 아니라 판단 품질**을 보상한다. "여진 관망(급등 후 추격 안 함)"·"루머에 회의(미확인 루머에 진입 안 함)"·"급등 후 매수 자제" 같은 **비추격 판단 미션**을 신설한다. 반대로 **"속보 N건 읽기"류 속보 소비량은 KPI·미션 목표로 절대 삼지 않는다**(속보 노출↑을 목표화하면 게임화 자기모순 — 도박 조장). 알림 착지 화면 = 서사(뉴스+차트)이며 **매수 시트 프리필 금지**(§7.4).

### 8.3.1 [신규] 개인 평생 실력지표·성과-정직 서피스 — 정본 game-modes §2.5

- **개인 평생 실력지표:** 리셋에 지워지지 않고 누적되는 **리스크조정 복합스코어(TWR 기반) 히스토리** 1개를 신설한다. 리셋 = '새 시즌' 시작이되 실력 트랙은 계속 축적 → (1)활동·수집과 무관한 '진짜 연습 피드백', (2)대회 진입 명분('네 실력지표가 리그 입장선 도달'), (3)reset-gacha 파밍 무력화(누적 지표는 운뽑기로 못 씻음).
- **정직성 워터마크:** 클라 결정론(§11.1)으로 생성된 개인 성과는 공유 카드·프로필에서 **'비검증(연습)' 워터마크를 강제**한다. **'검증 실력지표'는 서버 권위 대회 성과에만** 부여(GRF-11 확장) — 클라 시드 예지로 조작된 '완벽 이력'이 대회 진입 전 신뢰(herding·사칭)를 훼손하는 것을 차단.

### 8.4 리더보드·리그
- **절대수익률 단일 순위 금지.** 복합 스코어(수익률×리스크조정계수 + 분산·규칙준수, 손실 구간엔 리스크계수 곱 미적용해 부호역전 방지). 순위 옆 MDD·최대 단일종목 비중 병기.
- **단일 글로벌 순위 → 다차원 리더보드(SOC-04, 정본 social-profiles.md §6):** 리더보드는 단일 글로벌이 아니라 **시장(US/KRX/JP/코인) × 상품군(현물/ETF·ETN/옵션/선물·무기한/레버리지·인버스) × 레버리지 티어(1x/저배율/고배율) × 기간(일간/주간/시즌/전체) × 정렬(복합스코어/TWR/규칙준수/학습·배지)** 의 다축 필터로 탐색한다. 단, **탐색 축은 표시·필터일 뿐, 공식 순위·보상·승강은 GRF-01 복합스코어·GRF-05 리그·GRF-06 세그먼트로만 확정**('탐색은 자유, 보상은 공식'). 각 뷰 상단에 '보상 반영 여부'를 라벨.
- **레버리지/파생/옵션매도 고위험 리그 격리(GRF-16, IP1):** 레버리지(>임계, 예 3x)·파생·옵션매도 계좌는 **하이롤러/고위험 리그 전용 세그로 분리 집계**(현물 보상 순위 오염 차단, 형평성 하중 해소). 메인·현물 리그엔 **레버리지 상한**을 두고, 절대수익 정렬은 고위험 격리 리그에서만 허용(강한 면책 강제).
- **복합 스코어 레버리지 페널티·노티널 정규화(GRF-16):** 메인 점수 S에 레버리지 페널티 `Plev = w·max(0, peakLeverage−1)` 가산, 위험조정계수가 레버리지 부풀린 MDD로 자연 페널티. **노티널정규화 수익률(수익/평균노티널)·샤프 병용**으로 풀레버리지 도박의 1등 등극 무력화. 프로필/리더보드 분자는 확장 equity(NLV), 분모는 여전히 $100k 고정.
- **리그:** 약 30명 소규모 풀 + 주간 승급/강등. 초보/숙련·쉬운/리얼·(향후)소액모드 분리.
- **지표 게이밍 주의(2차효과):** 인위적 저변동 후 막판 몰빵, HHI 채우기용 무의미 분산 → 기간 일관성·규칙준수율 가중으로 게이밍 저항성 확보.
- **시뮬 통계 유효성:** 평균회귀 시계열의 음의 자기상관으로 샤프 과대평가 위험 → 스냅샷 절대시각 고정, 최소 표본·장기 구간에서만 샤프 계산.

### 8.5 대회·소셜
- **비공개 그룹 대회(최우선 소셜·K-factor 엔진):** 방장이 파라미터화, 서버 시드 결정론 시세. Free도 소규모(≤3명) 1개 개설 허용(바이럴 앵커).
- **유료 참가비+현금 상금 대회 최소 1년 배제**(복권 3요소·랜덤워크 실력논거 취약).
- **바이럴 앵커:** '수익률 자랑'과 **'초대 링크(그룹 대회 참가)'** 를 함께 민다. 공유 카드는 배지·레벨·리그 티어·대회 성적 중심, 하단 면책 강제(가드레일 ②). K-factor 목표는 초대 루프 기준으로 산정.
- **활동 피드:** 배지·레벨업·승급·스트릭·챌린지 완주·대회 승리 노출. 절대수익 자랑은 노출 절제(herding·사행 프레임 완화). 자동 카피트레이딩 금지, 인사이트는 **일반·비개인화**만 허용(개인 맞춤 매수 추천 금지 — 가드레일 ③).
- **신고·차단·모더레이션(소셜 v1 하드 선행 게이트):** 모든 UGC·소셜 노출 표면(공유 카드, 활동 피드, 그룹 대회, 친구·닉네임)에 **신고 → 모더레이션 큐 → 차단/제재** 파이프라인을 표준 기능으로 편입한다. 이는 소셜 v1의 **하드 선행 요건**(LEG-06 정합)으로, 파이프라인 미비 시 해당 소셜 표면을 공개하지 않는다. 실존 자산 매수 선동·시세조종성 게시는 금지(크립토 실데이터 후 모더레이션 강화, §13.4). 상세 흐름은 use-cases.md(UC-B29~B31) 참조.

#### 8.5.1 소셜·프로필·랭킹 (SOC 신규 도메인 #15 — 정본 social-profiles.md)

오너 확정 신규 스코프인 "타 유저 프로필 열람 + 공개범위 + 팔로우 + 다차원 리더보드"를 **신규 SOC 도메인(SOC-01~09)** 으로 도입한다. **GRF는 공정성 SSOT, SOC는 표면·관계·표시 레이어**로 GRF를 재정의하지 않고 참조·확장한다. IP1부터 프로필/랭킹 코어(SOC-01·02·03·05·06·07·08 + SOC-04 리그 분리)를 탑재한다.

- **프로필 열람(SOC-01):** 앱 내 유일한 '타 유저 열람 표면'(리더보드 행·활동 피드·공유 카드가 모두 이 프로필로 딥링크). 대표 지표는 리스크조정 복합스코어·리그 티어·배지·TWR 실력지표이며, **절대수익·절대자산은 대표 자리에서 숨김**($100k 고정분모).
- **필드단위 공개범위(SOC-02):** 계정 3티어(전체공개/팔로워공개/비공개) × 필드별 토글. **기본값 = 부분공개**(성과·배지 공개 / 실시간 포지션·거래내역·청산 이력 비공개). **서버 RLS 강제**(비공개 필드는 응답 페이로드에서 제외).
- **비대칭 팔로우(SOC-03):** follow(단방향 구독)와 friend(상호 승인)를 분리. **팔로우는 열람·피드 권한만, 순위·보상에 어떤 가중도 0**(popularity-to-win 금지) → 가짜 팔로워 파밍의 순위 유인 원천 제거.
- **성과 표시·리스크 투명(SOC-05):** 대표 3종(복합스코어·TWR·리그 티어) + **리스크 투명성 카드에 최대 사용 레버리지·청산(liquidation) 건수 요약·파생 노출 비중 병기**. 리스크 요약은 공개 프로필에서 숨김 불가이며, **상세 청산 이력은 기본 비공개**로 두고 공개 시에도 건수·위험 요약만 허용하며 조롱·자랑 브로드캐스트를 금지한다.
- **anti-herding/사칭/시빌(SOC-06/07):** 실시간 고배율 포지션 기본 비공개·원클릭 카피트레이드 미제공(§1.4). 핸들 유일성·homoglyph 사칭 탐지·검증 배지·시빌 그래프(GRF-10)로 가짜 팔로워·사칭 차단.
- **미성년 안전(SOC-08):** 강제 최소공개·PII 미표시·프리셋 아바타·성인-미성년 상호작용 최소화(DP-13·LEG-05 정합, 사용자 설정·A/B로도 상향 불가 하드 오버라이드).
- **신규 유즈케이스 UC-B32~B35 참조**(프로필 열람·공개범위 설정·팔로우/친구·다차원 리더보드 탐색; use-cases.md는 별도 편집). 상세 정책문·매트릭스는 정본 [social-profiles.md](./social-profiles.md) §5~§8.

#### 8.5.2 개인/대회 2모드 대회 UX 정합 (정본 [game-modes.md](./game-modes.md))

> 아래는 대회 모드의 라이프사이클·참여·관전·공유·개인 연동을 **PRD 표면 요건**으로 요약한 것이며, 상세 상태·스키마·어뷰징 방어는 game-modes.md를 정본으로 참조한다(중복 서술 금지).

- **개인→대회 다리(game-modes §1.5·§2.4·§8.2):** 개인 미션/시나리오 클리어가 **'이번 주 리그 도전' 원탭 CTA**를 발화하고, 첫 미션 완료 후 **주간 공식 리그에 opt-out 자동 등록**(결정 없이 디폴트 경로가 대회에 착지, 친구 불필요). 격리만 있고 다리가 없으면 개인모드는 이탈싱크가 되므로 이 후크는 하드 요건이다.
- **대회 라이프사이클(game-modes §3.4):** `DRAFT → RECRUITING → LIVE → SETTLING → ARCHIVED/HALL_OF_FAME` 상태기계. **방장 잠적 시 고정 파라미터로 자동 진행·정산**(방장 의존 제거), **SETTLING은 서버 권위 재계산으로 최종순위 감사 확정**. 그룹 대회 성과는 글로벌 티어 미반영, 공식 시즌 대회만 명예의전당 글로벌 트랙 등재.
- **참여 게이트(game-modes §4.3):** 조건 미리보기 카드 → 가입·연령게이트·**전면 면책 동의(대회 진입 직전 트리거, §7.2·§13.2)** → 원탭 참가 → 서브계좌 생성·서버 시드 스트림 합류. **고위험 대회(레버리지>임계·공매도·파생)는 '원금 초과 손실 가능' 고지를 규칙과 세트로**(GRF-16·§13.4).
- **관전 모드(game-modes §4.4):** 라이브 순위·남은 시간·규칙 배지·집계 요약만 공개, **타 참가자 실시간 포지션·주문은 비공개**(SOC-06 herding 방어). 진행 중 보상 대회 리더보드는 **비참가 관전자에겐 지연·대략화**(담합 실시간 모니터링 차단). 관전자는 **매매 불가**(서브계좌 미생성) + '참가하기' CTA 상시 노출.
- **공유·상품 UX(game-modes §4.5·§5):** 공유 카드는 **대회 성적·배지·티어 중심 + 모드 라벨 강제 + 하단 미세 면책**, 절대수익 자랑 절제(SOC-06). **모든 대회 보상에 '현금 아님' 라벨을 코드레벨로 강제**(LEG-04).
- **콜드스타트 방어(game-modes §4.6):** 로스터 독립형 **하우스·테마 시즌 대회**(결정론·비동기, 시스템 고스트봇 상대 — '**시스템 상대**' 명시)를 **MVP부터 상시 운영**해, 유저 대회 0개여도 디스커버리 상단을 공식 대회가 채운다. 30인 라이브 리그는 세그먼트 유동성 확보 후로 유보.
- **상품 정책 v1(game-modes §5, §13.1 R3):** v1(최소 1년) 전면 **비현금·무참가비**. **참가비/상금액/pot/rake 파라미터는 UI·API·DB 어디에도 만들지 않는다**(구조적 차단). 실제 상품은 v2+ 스폰서 대회(참가비 0 + 스폰서, 법률 게이트 통과 시)로만 도입하며 **rake는 절대 금지**.
- **anti-cheat 8종(game-modes §3.5·§6.5):** 2축 계좌 모델(`contextType × frictionMode`)·대회별 유니크 시드(`seed=H(competitionId, serverSecret)`)·reEntry 시드 재분기(재입장은 항상 비순위)·검증계정 게이트(≥5 검증계정, 미달 시 비경쟁 연습 라벨)·XP 소스 태깅(고위험 게이팅은 검증 진행만 카운트) 등 8개 방어를 채택한다.

### 8.6 서버 의존성 & 임계 선행작업
소셜/리그는 서버 없이는 구현 불가(로드맵 종속성). 임계 선행: **다계좌 리팩터**, **서버 사이드 이벤트 검증 로그**(치팅 방지), **서버 UTC 시계**(스트릭·시즌·마감 판정). 하이롤러 리그 등 규칙 변경은 **시즌 종료 후 적용**(시즌 중 규칙 불변).

---

## 9. 선택적 팁·툴팁

> 이 제품은 교육 앱이 아니라 **재미 게임**이다. 학습은 **핵심이 아니며**, 원하는 유저를 위한 **선택적·비강제 팁** 수준으로만 존재한다. 어떤 팁도 온보딩·진행을 막지 않고 언제든 스킵할 수 있다. 리텐션은 팁이 아니라 게임 콘텐츠(§8)로 만든다.

### 9.1 팁의 범위와 금지선
- **가벼운 팁(선택):** "분산이 뭔가요", "손절 주문은 이렇게", "이 종목은 왜 이렇게 움직일까" 같은 30초 툴팁·용어 설명을 매매/포트폴리오 화면 **인라인(맥락형)** 으로만 제공. 별도 학습 탭·커리큘럼·수료 트랙·학습성과 측정은 없다.
- **법적 금지선(가드레일 ③):** 개인 맞춤 매수 추천·"지금 X 사라"·종목 예측·목표주가·개인화 실시간 시그널은 **어떤 형태로도 제공하지 않는다.** 팁은 일반론·비개인화만. 종목명은 예시로만, "일반 정보·비자문" 라벨 고정.

### 9.2 전달 방식
- **맥락형 툴팁:** 매매/포트폴리오 용어에 점선 밑줄 → 30초 카드 바텀시트(별도 탭 격리 안 함, 스킵 가능).
- **선택적 팁 카드:** 챌린지 탭 안의 가벼운 컬렉션(24장 내외). 읽으면 소량 코스메틱 보상이 있을 수 있으나 **게임 진행에 필수가 아니다.**
- **시나리오(재미 콘텐츠):** `priceEngine` 래핑 `ScenarioEngine`으로 급락장·변동성 폭발·섹터 로테이션을 **재미있는 도전 모드**로 재생("급락장에서 살아남기" 챌린지 등). "가상 시나리오" 라벨.

### 9.3 콘텐츠 운영(경량)
- **Content-as-Data:** 팁 카드·용어·시나리오·튜토리얼을 JSON/MDX로 관리. 번들 기본값 + 원격 오버라이드(앱 재심사 없이 갱신, 오프라인 폴백). v1은 Git 기반(PR=검수 게이트).
- **법무 검수 게이트(머지 필수):** 개별 매매 지시·예측·수익보장·실시간 시그널 금지, '일반 정보·비자문·모의' 라벨 상시(가드레일 ②③). AI 도우미는 사전정의 답변 우선 + 자유생성 범위 제한 + 오정보(hallucination) 면책.
- **콘텐츠 물량은 게임 콘텐츠로 해결:** 리텐션은 팁이 아니라 **미션·챌린지·시즌·대회·수집** 로테이션으로 확보한다(§8). 무거운 학습 인력(교수설계자·SME 전담)·문항은행·SRS·학습성과 대시보드는 **범위에서 제외**한다.

---

## 10. 기술 아키텍처·스택 결정과 근거

### 10.1 스택 결정

| 영역 | 결정 | 근거 |
|---|---|---|
| 런타임 | Expo 유지, Expo Go→**dev-client** | 광고/IAP/MMKV 네이티브 모듈 필요 시점에 선제 전환 |
| 내비게이션 | **Expo Router** | 그룹/대회 초대 딥링크·웹 동일 라우트 = K-factor load-bearing |
| 상태관리 | **Zustand(클라)+TanStack Query(서버)** | 2초 tick 전 consumer 재렌더 폭풍 해소(selector 구독) |
| 로컬 저장 | **MMKV(핫 KV)+expo-sqlite(원장·시계열)**, AsyncStorage 폐기 | 무한 성장 blob·부분쓰기·경쟁 제거. 웹은 StorageProvider 추상화(IndexedDB 폴백) |
| 차트 | **victory-native XL(Skia)** + Skia 스파크라인 | New Arch·웹 호환 |
| 도메인 | **순수 TS `domain/` 레이어(RN 의존 0)** | 클라/서버 동일 실행 → 대회 공정성·치팅방지·"실력 콘테스트" 근거 |
| 백엔드-데이터평면 | **Supabase(Postgres+Auth+RLS+Realtime+Edge Functions)** | Auth/게스트→가입, **RLS 데이터 경계**, **이벤트소싱 원장**(append-only), 리더보드 **집계 SQL·머티리얼라이즈드 뷰**, 정적 조회. 실시간 시세 스트림은 **여기서 하지 않는다**(§10.3) |
| 백엔드-실시간평면 | **상시구동 워커(long-lived) + Redis(또는 대체)** — Fly.io/Railway/Render 상주 컨테이너 또는 Cloudflare Durable Objects | 거래소 WS 업스트림 흡수, **시세/팬아웃 워커**, hot-cache·요청병합, 대회 틱 스트림, 서킷브레이커 상태, 이상치 2-틱 버퍼(§10.3). 솔로는 미경유, **소셜/대회를 켤 때만 진입** |
| 수익화 | **RevenueCat(구독/IAP 크로스플랫폼 검증)+AdMob** | 서버 영수증 검증·엔타이틀먼트 |
| 배포 | **EAS Build 3프로파일+Update(runtimeVersion=fingerprint)+Submit+CI 게이트** | OTA는 버그픽스·콘텐츠 한정(심사 우회 배포 금지) |

### 10.2 아키텍처 원칙
- **`domain/` 순수 TS 레이어**에 주문엔진·시뮬레이터·P&L·샤프/MDD·코퍼레이트액션·`Money` 값객체(round-half-even 단일 규칙) 분리 → 클라·서버 동일 구현 공유(±1센트 발산 방지).
- **`MarketDataProvider` 인터페이스**(`getQuote/subscribe/getCandles`)를 지금 도입, `SimulationProvider` 첫 구현체. API 키는 **반드시 백엔드 프록시 경유**(키 은닉+캐시+요청병합+서킷브레이커→시뮬 폴백).
- **이벤트소싱 거래원장**(UUID+멱등키, append-only) + 아웃박스 동기화. 솔로=로컬 우선(오프라인 완전 작동), 대회=서버 권위.
- **다계좌·엔타이틀먼트 게이팅**을 v1 진입 시 선추상화(현행 단일 로컬계정 리팩터).
- **확장성:** FlashList v2 + 뷰포트 기반 시세 구독(가시/보유 종목만 고빈도), SQLite 마이그레이션 러너.

### 10.3 백엔드 아키텍처 현실 정합 — 상시 워커 vs 관리형, 결정론 재계산 비용

> **왜 이 절이 필요한가.** §10.1은 백엔드를 "Supabase(Postgres+Auth+RLS+Realtime+Edge Functions)"로 단일 확정했었고, 시세 결정론의 부수효과로 "과거 임의 시각 가격을 **O(1) 재계산**"할 수 있다는 가정이 있었다. 두 진술 모두 정책이 요구하는 실시간 대회·리더보드와 채택한 시뮬 엔진(§11.2)의 실제 시간복잡도 앞에서 무너진다. 이 절은 두 결함을 규칙·수치로 못박아, 실시간 대회·비용모델·동기화 설계가 잘못된 전제 위에 쌓이지 않게 한다. 상세 근거는 [planning-review.md](./planning-review.md).

#### 10.3.1 결함 A — 관리형 stateless로는 정책이 요구하는 상시 스트림을 호스팅할 수 없다

정책(MKT-07)은 "서버가 **업스트림 1개 구독 → N 유저 브로드캐스트**"(WS 팬아웃), "인기 종목 **hot-cache TTL 1~5초**", "대회 시작 시 **동시 구독 폭주 큐잉**"을 요구한다. 이들은 전부 **프로세스가 틱과 틱 사이에 살아 있어야(stateful, long-lived)** 성립한다. 반면 Supabase Edge Functions는 요청당 부팅·수십 초 실행상한의 **stateless FaaS**이며, Supabase Realtime은 "Postgres 행 변경 브로드캐스트"이지 "외부 WS를 흡수해 재분배하는 팬아웃 게이트웨이"가 아니다.

| 정책이 요구하는 것 | 필요한 실행형태 | Supabase Edge/Realtime 단독 가부 |
|---|---|---|
| 업스트림(거래소 WS) 1구독 → N유저 팬아웃 (MKT-07) | 지속 연결을 쥔 **상시 프로세스** | ✗ (Edge는 요청 종료 시 연결 소멸) |
| hot-cache TTL 1~5초, 요청병합(fan-in) (MKT-07) | 인메모리/Redis **공유 상태** | ✗ (Edge 인스턴스 간 상태 공유 없음) |
| 대회 틱 스트림 서버 권위 팬아웃 (MKT-01/12) | 틱 루프를 도는 **워커** | ✗ |
| 서킷브레이커 half-open 히스테리시스 (MKT-07) | 프로세스 수명 넘는 **카운터** | △ (DB 왕복 시 지연·비용) |
| 이상치 2-틱 확인 로직 (MKT-08) | 직전 틱을 **기억하는 상태** | ✗ |

**결정(§10.1 백엔드 행 반영):** 백엔드를 **역할별 2계층**(데이터평면 / 실시간평면)으로 분리 명시했다(§10.1 표). 하나의 관리형 서비스에 실시간 스트림을 억지로 태우지 않는다. 담당 범위는 §10.1 표의 두 행과 같다.

- **Redis 대체 허용:** 초기 규모에선 단일 워커 프로세스 인메모리 맵으로 시작 가능(수평확장 0). Redis는 **워커 2대 이상으로 수평확장하거나 대회 동접이 게이트(10.3.3)를 넘는 시점**에만 도입 — 관리형 Upstash(요청과금) 또는 워커 사이드카.
- **비용 하한이 0이 아님을 재무모델에 반영:** 상시 워커는 최소 1대가 24/7 과금(대략 $5~25/월/워커 + Redis)이다. 손익분기 역산·§12 수익화는 이 **인프라 고정비 하한**을 전제로 다시 계산한다(§12.6). 현재 "솔로는 백엔드 0"은 여전히 참이나, **소셜/대회를 켜는 순간 0이 아니다**.
- **솔로 경로는 무변경:** 솔로(연습) 모드는 클라이언트 결정론(§11.1)으로 백엔드 0 유지. 실시간평면은 **대회/리그/리더보드/실데이터**를 켤 때만 진입한다.

#### 10.3.2 결함 B — "O(1) 재계산"은 신화다. 채택 엔진은 O(tickIndex)

"과거 임의 시각 가격 O(1) 재계산"은 결정론의 공짜 이득처럼 인용됐으나, 이는 **가격이 tickIndex의 무상태 순수함수**일 때만 참이다. 그러나 §11.2가 채택한 엔진은 **경로의존(path-dependent)** 이다.

- **GARCH-lite 변동성:** `σ_t² = ω + α·r_{t-1}² + β·σ_{t-1}²` — σ_t가 σ_{t-1}에 재귀 의존.
- **평균회귀/EMA 추세 진동:** EMA 자체가 전체 과거의 지수가중합.
- **상관 팩터 상태:** 공통 시장팩터·섹터팩터 레벨이 스텝마다 누적.

따라서 `price(instrument, T)`를 얻으려면 시작점부터 T까지 **모든 틱을 순차 전개**해야 한다 → 실제 복잡도는 **O(T − T₀)**, 즉 O(tickIndex). "O(1)"이 아니다.

**이 신화가 부수는 것 — 구체 수치(TICK_MS=2000, 즉 0.5틱/초):**

| 시나리오 | 재계산 틱 수(앵커 없음) | 종목 M=50 시 총 반복 |
|---|---|---|
| 앱 6시간 방치 후 복귀 | 6h×1800 = **10,800** | 540,000 |
| 주말(48h) 방치 크립토 24/7 | 48h×1800 = **86,400** | 4,320,000 |
| 장기 미접속 3개월 크립토 | 90d×43200 = **≈3,888,000** | **≈194,000,000** |

한 화면을 그리려 종목당 수백만 번의 GBM+GARCH 반복 → 콜드스타트 프리즈·배터리 소진·대회 서버 CPU 폭발. "결정론이면 저장 대신 재계산" 문장은 이 비용 없이는 성립하지 않는다.

**결정 — 앵커 체크포인트를 엔진 불변식으로 의무화:** 결정론 엔진은 **주기적 앵커 스냅샷**을 남기고, 모든 재계산을 **최근 앵커 ~ 현재**로 **클램프**한다.

- **앵커 스냅샷 페이로드(종목별):** `{ tickIndex, price(정수 최소단위), σ_t², factorState(공통·섹터 레벨), rngState(PRNG 내부상태), seedVersion, epochAnchorMs }`. 이 7요소가 있으면 어떤 시각도 앵커에서 이어 전개 가능.
- **앵커 케이던스:** `ANCHOR_INTERVAL_TICKS = 450`(2초틱 기준 15분) **또는 세션 경계 중 먼저 오는 것**. 즉 앵커는 `max(직전 세션개장, 15분마다)`.
- **재계산 상한 보장:** 어떤 콜드스타트도 **최대 450틱 × M종목**만 전개 → 위 표의 3개월 케이스가 종목당 4M회에서 **450회**로 축소(약 8,600배 감소). 실질 O(1) 상한을 규칙으로 만든다.
- **자산군별 앵커 규칙(세션모델과 결합):**
  - **EQUITY/ETF:** 마감 중 틱 정지 → σ·팩터상태가 동결되므로 **세션 개장이 자연 앵커**. 재계산은 당일 개장~현재로 자동 클램프.
  - **CRYPTO 24/7:** 세션 경계가 없어 자연 앵커가 **영원히 안 옴** → **15분 강제 앵커를 필수**로 명시(강제 없으면 위 3개월=4M 반복 케이스가 그대로 현실화). 이 한 줄이 이 절의 가장 load-bearing한 규칙이다.
- **저장 위치(신뢰 경계별):**
  - 솔로/클라: 최근 앵커 1개를 MMKV에 로컬 저장(§10.1). 콜드스타트 시 `nearestAnchor ≤ now`에서 전개.
  - 대회/서버: 앵커를 Postgres에 **append-only로 영속**(감사·분쟁 재생성 요건 충족). 분쟁 리플레이 = `앵커 + seed`로 결정론 재생.
- **감사 정합:** 앵커는 seedVersion을 담으므로 §11.1의 "seedVersion pin"과 충돌 없이 버전 경계에서 새 앵커 라인 시작.

#### 10.3.3 결함 C(재무·용량) — 대회 부하는 산정 대상이며 로드맵 게이트다

서버 도입을 "언제"만 묻고 "**어떤 형태**"(관리형만 vs 상시 워커 포함)와 "**어느 부하까지**"를 안 물으면, 소셜을 켜는 순간 미검증 용량으로 진입한다. 대회 부하를 **파라미터화된 산정식**으로 고정하고 **로드맵 게이트**로 건다.

**부하 산정식(동시 진행 대회 기준):**

```
활성종목 가격연산/초   = Σ대회( 유니버스종목수 M ) × (1 / TICK_초)      # 팬아웃 덕에 참가자 수와 무관
WS 팬아웃 메시지/초     = Σ대회( 동접참가자 × 1인구독종목 ) × (1 / TICK_초)
리더보드 재계산/초      = Σ대회( 동접참가자 × 평균보유종목 ) × 마크투마켓빈도
동시 WS 연결 수        = Σ대회( 동접참가자 )
```

**기준 시나리오(TICK_초=2, 대회당 참가 30명·동접 50%·유니버스 M=50·1인 구독 20종목·보유 8):**

| 동시 대회 N | 가격연산/초 | 팬아웃 msg/초 | 동시 WS 연결 | 판정 |
|---|---|---|---|---|
| 10 | 250 | 1,500 | 150 | 단일 워커 인메모리로 충분(Redis 불요) |
| 100 | 2,500 | 15,000 | 1,500 | 워커 1~2대·Redis 도입 라인 |
| 500 | 12,500 | 75,000 | 7,500 | 워커 수평확장·Redis 필수·큐잉(MKT-07) |

- **핵심 통찰:** 팬아웃 구조(MKT-07) 덕에 **가격연산은 참가자 수와 무관**(종목당 틱 1회)하지만, **네트워크 egress와 WS 연결 수는 동접에 선형**이다. 병목은 CPU가 아니라 **연결·egress**다 → 워커 스펙은 vCPU가 아니라 **동시연결·대역폭 기준**으로 사이징한다.
- **대회 시작 스파이크(MKT-07 "동시 구독 폭주"):** N개 대회가 정각 동시 시작하면 순간 구독요청이 위 정상치의 수십 배 → **백엔드 큐잉+우선순위(보유>가시>기타)** 를 게이트 통과 조건으로 명시.

**로드맵 게이트(§17 소셜/대회 마일스톤 진입 전 필수 통과):**

| 게이트 | 통과 기준 |
|---|---|
| G1 부하 재현 | 위 "N=100" 시나리오를 합성부하로 재현, p99 틱 지연 < 250ms |
| G2 팬아웃 검증 | 업스트림 1구독 → 30유저 브로드캐스트 실측(심볼 한도 내), 중복 업스트림 0 |
| G3 앵커 콜드스타트 | 크립토 48h 방치 계좌 콜드스타트 재계산 < 100ms(앵커 클램프 실측) |
| G4 비용 하한 | N=100 시 워커+Redis 월비용 산정치를 §12.6 손익분기에 반영 |
| G5 큐잉 | N개 대회 동시 시작 스파이크에서 드랍 0·우선순위 순서 보장 |

> **관련 정책 반영:** policies.md의 MKT-01(재계산 복잡도·앵커 체크포인트), MKT-07(상시 워커+Redis 전제·큐잉)에 본 절의 규칙을 규칙/사이드이펙트로 반영한다. 상세 근거는 [planning-review.md](./planning-review.md).

---

## 11. 데이터·시세 전략

### 11.1 시세 권위·결정론
- 기기-로컬 `Math.random()` → **`price=f(seed,ticker,tickIndex)` 서버/NTP 절대시각 순수함수.** 솔로=클라 결정론(비보상), 대회·리그=**서버 은닉 시드**(미래가 예측 치팅 차단). 로컬 결정론 시세로 만든 프로필 실력지표·공유 카드는 순위 미연동·신뢰 경계 명시.
- **결정론 재계산은 O(1)이 아니라 O(tickIndex)다(§10.3.2).** 채택한 GARCH-lite·평균회귀·팩터 상태가 순차의존이므로, 시세를 '저장 안 하고 임의 시각 O(1) 재계산'한다는 가정은 폐기한다. 대신 **주기적 앵커 체크포인트(15분/세션경계, `{tickIndex·price·σ_t²·factorState·rngState·seedVersion·epochAnchorMs}` 7요소 스냅샷, §10.3.2)** 를 남기고 재계산을 **'최근 앵커~현재'로 클램프**한다(재계산 상한 ≤ ANCHOR_INTERVAL 틱). **크립토 24/7은 세션 앵커가 없어 15분 강제 앵커가 필수**다. 세션 경계에 틱이 멈추는 EQUITY/ETF는 세션 개장이 자연 앵커가 된다.

### 11.2 현실성 캘리브레이션
- 현행 `shock=U(-1,1)*(vol/100)`은 시간축 오정규화. → 로그정규 GBM, 연환산 σ(가상 대형주 예: Nyxor Systems/NYX 0.30 등)·소폭 드리프트(+7%/년, 상한). **주의:** 데이터 엔지니어의 "연 595%·20배 과변동"은 평균회귀 제거 시 무제약 확산치이며, 실제 현행 엔진은 κ=0.02 평균회귀로 밴드에 갇힘(저변동 종목은 오히려 실현 변동이 작음) → GBM 전환 시 annualVol을 **실현 목표로 캘리브레이션** 필수(내부 모순 회피).

### 11.3 소스 티어링 로드맵
| 단계 | 미국주식 | 크립토 | 한국/기타 | 비용 |
|---|---|---|---|---|
| P0(MVP) | 시뮬(결정론) | 시뮬 | 시뮬 | $0, 규제 최소 |
| P1(베타) | Finnhub 무료(내부 테스트만) | 거래소 WS(**약관 개별 법무검토 후**) | 시뮬/지연 | $0(재배포 미허용 주의) |
| P2(정식) | Polygon/TwelveData 재배포 유료 | 거래소($0) | KRX 지연/일봉 | $30~200/월 |
| P3(스케일) | Polygon 실시간 재배포 | 거래소 WS | KRX 실시간(별도 견적) | $200+/월 |

> **'비용' 열 각주:** 위 데이터 라이선스 비용은 **호출량 과금 + 팬인/캐싱 전제(시나리오 A)** 의 값이다. per-subscriber 계약이면 MAU에 비례해 폭증하며, 실시간 전송(WS egress) 인프라는 별도로 경쟁모드 동접에 비례한다. 손익분기·스케일 모델은 **§12.6** 참조.

- **재배포 게이트:** 무료 티어 실시간의 공개 배포는 대부분 약관 위반 → 정식 출시는 재배포 명시 허용 플랜만. 크립토 "재배포 완화" 가정은 **계약 조항 확인 전까지 폐기.** 실데이터 대회는 시드 재생성 불가하므로 **녹화된 서버 권위 틱 로그(해시체인 감사)** 를 진실원본으로.
- **오인 방지:** `Quote.source/delayedMs` 배지("실시간/△분 지연/시뮬"). 지연 데이터 실시간 오인 표시 금지. 단 "15분 지연" 배지가 몰입을 갉을 수 있음(2차효과) → 노출 절제.
- **시장 세션:** MVP는 24/7 시뮬 기본 옵션, `SessionSpec` 필드 예약. 24/7에서도 UTC '거래일' 경계로 dayOpen·DAY TIF 정의.

### 11.4 멀티마켓·파생 시세 (신규 스코프 — 정본 instruments-and-markets.md §3·§5·§6)

- **3거래소 ExchangeSpec·세션캘린더(MKT-19, IP3; US 단일 IP1 관통):** `{tz(IANA)·통화·정규장·특이사항·가격제한·결제}` 레지스트리.
  - **US**(NYSE/NASDAQ, America/New_York, USD): 09:30–16:00 ET, **DST 준수**, 프리/애프터, LULD ±5/10/20% + MWCB 7/13/20%, T+1.
  - **KRX**(Asia/Seoul, KRW): 09:00–15:30 KST, DST 없음, 개장/종가 동시호가, **±30% 상하한가**(기준=전일종가)·**VI**, T+2.
  - **JP**(TSE/JPX, Asia/Tokyo, JPY): 09:00–11:30·12:30–15:00, **점심 휴장**, **値幅制限**(고정 엔화 밴드) ストップ高/安, T+2.
- **세션 상태머신·DST(MKT-20/21):** 세션 상태(PRE/OPEN/LUNCH/AUCTION/POST/CLOSED)는 **데이터(캘린더) 주도**, 판정은 거래소 tz 로컬시각으로 매핑하되 **저장·비교는 서버 UTC**(기기시각 불신). **DST는 IANA tz DB에 위임**(자체 오프셋 계산 금지), 전환일 property test로 1시간 오차 방지. 동시호가·오버나이트/런치 갭은 결정론 주입.
- **가격제한·서킷브레이커(MKT-21):** KRX 상하한가 밴드 하드클램프(상한가/하한가 '락')·VI 2분 단일가, JP 値幅 밴드 클램프·밴드밖 거부, US LULD 5분 단일가·MWCB 전종목 HALT.
- **FX 결정론 GBM 24/5(MKT-22, IP3; FxProvider 예약 IP1):** 통화쌍 USD/KRW(~1,350)·USD/JPY(~150), base=USD. 환율 = **결정론 GBM(저변동 annualVol≈0.08~0.12 << 주식) + 평균회귀**. **FX 세션 24/5**(주말·공휴일 금 종가 고정), 크로스레이트는 **USD 경유 삼각환산 1회 곱**. 정수 마이크로(1e8) 저장.
- **파생 3층 가격(정본 §3):** PERP/FUTURES는 **인덱스(공정 기초) → 마크(median-guard 공정가) → 라스트(체결가)** 3층 분리. **청산 판정=마크, 체결=라스트.** 인덱스 결측 시 마지막 유효 인덱스로 VM 0·'가격대기' 라벨(avgEntry 폴백 금지).
- **펀딩·정산가(MKT-16/17):** 무기한 **펀딩레이트 8h(00·08·16 UTC)** 제로섬(Σ롱+Σ숏=0), 선물 **일일정산가(DailySettle)·최종결제 SQ**·베이시스/기간구조. 모두 서버 UTC 절대시각 산출.
- **ETF iNAV·괴리(MKT-18):** 현물 ETF의 **iNAV(장중지표가치, 15초 산출)·괴리(Premium/Discount)·추종오차**. 레버리지/인버스 ETF는 일일리셋(경로의존·변동성감쇠 재현, ±3x 상한), 운용보수는 현금흐름 아닌 NAV 드래그. ETN은 발행사 신용·조기상환(acceleration)·지표가치.
- **결정론·서버 권위:** 인덱스·마크·펀딩·정산가·SQ·basis·ETN 지표가치·IV·FX레이트·청산가는 모두 결정론 시드(솔로) 또는 서버 권위(대회) 산출, 클라 임의값 금지.

---

## 12. 수익화·비즈니스 모델

### 12.1 모델 한 줄
> 무과금 90%+를 리워드 광고로 보조 수익화하고, 소수 고관여를 **분석·소셜·꾸미기·편의** 프리미엄으로 전환하며, 규제 청정 **B2B/커뮤니티 시트 라이선스(옵션)**를 매출 안정 채널로 병행하는 하이브리드. (브로커 아님 → "실계좌 전환 미끼" 모델 원천 봉쇄, 실시간 데이터는 프리미엄 앵커로 쓰지 않음.)

### 12.2 무료/프리미엄 경계
- **영구 무료:** 쉬운 모드 매매, 기본 종목, 데일리 미션·스트릭·배지, 주간 리그, 그룹 대회 참가, 기본 성과지표 3종, 계정 삭제·데이터 이전. (핵심 재미·매매 루프는 인질로 잡지 않음.)
- **Plus:** 광고 제거, 고급 성과분석, 백테스트 확대, 리얼 모드 마찰·OCO/고급 조건부 주문, 커스텀 시나리오, 다계정(3~5), 대규모 대회 개설, 월 코스메틱.
- **Pro/Max(v2):** 무제한 백테스트, AI 코치, 대규모 대회, 독점 코스메틱.

### 12.3 가격·광고·IAP
- **가격:** Plus 월 $6.99 / 연 $41.88(연납 ~50% 할인), 7일 무료체험, **소프트 페이월**(바이럴·네트워크효과 우선). Pro/Max 월 $12.99 / 연 $89(v2). 지역가격 PPP(v2).
- **광고:** 리워드 비디오 주력(자발적 액션만, 매매 직후 전면광고·매매 축하 금지). 미성년·특정 관할은 **금융/도박/코인/CFD 카테고리 전역 차단** + 비개인화.
- **가상현금 리필 가드레일 — 두 파라미터 분리(정합):**
  - **일반 리필(수시):** 일일 상한 = 시작자산 **5%($5,000)**, 회당 $1,000~2,000, 쿨다운 4h, 하루 3회.
  - **파산회생 리필:** 파산(equity 임계 미만) 후 회생 시에는 별개 파라미터로 **일일 3%**를 적용한다(§14 ACC의 해제선 25%·히스테리시스와 결합). **일반 5% ≠ 파산회생 3% — 서로 다른 파라미터로 관리**(문서 전반 3%/5% 혼재 방지).
  - 코인으로 매매 원금 구매 금지. **손실 무게감 희석·loss-chasing 회피 위해 기본안은 광고 리워드를 코스메틱·부스터·체험권 중심으로**, 가상현금 리필은 최소화.
- **IAP:** 코스메틱·편의·시즌패스(미션·챌린지 진행으로 언락). 가상화폐 대량충전·유료 참가비·현금 상금 배제. 확률형 코스메틱은 확률 공시(한국 게임산업법)+천장, 슬롯머신형 사행 UX 배제.

### 12.4 유닛 이코노믹스 (보수적)

> **재무 SSOT:** ARPU·LTV·손익분기(BEP)·B2B ARR 등 **모든 재무 수치의 단일 소스는 [monetization-model.md](./monetization-model.md)** 다. 본 절은 그 값을 **전재(인용)** 만 하고 자체 값을 두지 않는다. 모든 수치는 별도 표기 없으면 **net**(스토어 수수료 SBP 15% + 환불·미수 ~3% 반영 후 ≈ gross×0.825)이며, `gross`는 유저 청구액으로 라벨을 붙인다.

- **광고 ARPU 보수 재추정:** 종전 광고 ARPU $0.10(gross 전제·가드레일 미반영)은 폐기. 리워드 가드레일·비개인화 폴백·미성년/학생 카테고리 차단 반영 시 **광고 net ARPU = 보수 $0.040 / 기본 $0.050 / 낙관 $0.070**.
- **구독 ARPU 정정:** 종전 "$0.06"은 라벨 부재·산술 오기였다. 정정값 = **구독 net $0.04**(= gross $0.05 = 인스톨→유료 2% × 결제자 월 net ~$2.5).
- **블렌디드 net ARPU 3시나리오(monetization-model.md §2.3 전재, net/gross 라벨 병기):**

  | 시나리오 | 구독(net) | 광고(net) | IAP(net) | **블렌디드 net ARPU** | $30k net MRR 도달 MAU(매출 마일스톤, ≠손익분기) |
  |---|---|---|---|---|---|
  | 보수 | $0.040 | $0.040 | $0.020 | **$0.100** | ~300k |
  | 기본 | $0.041 | $0.050 | $0.025 | **$0.116** | ~259k |
  | 낙관 | $0.045 | $0.070 | $0.030 | **$0.145** | ~207k |

- **라벨 정정(중요):** 위 마지막 열은 **"$30k net MRR에 도달하는 MAU"(매출 마일스톤)** 이지 **손익분기(BEP)가 아니다.** 종전 표는 이를 'BEP'로 오라벨(그리고 낙관 $0.18/167k 등 팬텀·라벨 밀림)했다. **실제 손익분기(monetization-model.md §4):** 인프라 단독 BEP는 **~5~7k MAU**(초기 생존 용이), 팀 인건비 포함 BEP는 **~190k(낙관)/242k(기본)/285k(보수) MAU**(B2B ARR 반영 시 ~24k MAU 하락). 종전 "$30k MRR에 150k MAU"는 gross $0.19 기준값으로, net 기준으로는 ~259k(기본)다.
- **함의:** **규모(오가닉·바이럴·B2B)가 수익화보다 선행 병목**이다(인스톨당 net LTV $0.63 → 유료 UA는 현 이코노믹스에서 성립 불가). **구독·B2B 중심 시나리오를 기본안으로** 재모델링한다(광고는 보조 업사이드, §16 O2). MRR 목표를 광고 낙관에 의존시키지 않는다.
- **앱스토어 수수료:** 초기 SBP 15%로 모델링, 연매출 $100만 돌파 시 Apple 30% 구간 시나리오. 웹 결제(~3%)는 반스티어링·EU DMA 개별 검증 후에만.
- **데이터·실시간 인프라 = 고정비가 아니라 리스크 축:** 데이터·실시간 인프라는 '관리 가능한 고정비'가 아니라 **과금모델(호출량 vs per-user)·심볼 유니버스·동접 스케일에 따라 변동하는 리스크 축**이다. 호출량 과금 + 캐싱 하에서는 데이터가 월 $199 내외로 평탄(MAU 무관)하지만, **per-subscriber(유저당) 실데이터 계약이면 MAU에 비례해 폭증**(150k MAU × $1.5 = 월 $225k → 매출 8배 초과, 영구 적자)한다. per-user 데이터 계약은 계약 게이트(§12.6 IC-6)에서 원천 차단한다. 손익분기 유저수 표와 스케일 모델은 **§12.6** 참조. 진짜 고정비 리스크는 UA와 **경쟁모드 실시간 전송(WS egress) 인프라**다.

### 12.5 B2B
- 앱 내 IAP가 아닌 **기업 계약·인보이스 연간 시트 라이선스**(스토어 수수료·자문 규제 우회). 학생 시트는 광고·결제·공개 소셜 코드레벨 비활성. 파일럿도 **DPA/학생데이터 처리계약 선체결 후** 시작.

### 12.6 인프라·실시간 데이터 비용 스케일 모델 (동접·데이터 과금·손익분기)

> **정합:** §12.4의 "데이터는 월 $30~200 관리 가능"은 **무조건이 아니라 과금모델·캐싱·스케일에 조건부**다. 호출량 과금 + 백엔드 팬인/캐싱 + 뷰포트 티어링이 모두 성립할 때만 참이며, per-subscriber(유저당) 계약이면 MAU에 비례해 폭증한다(정책 MKT-06 사이드이펙트와 정합). 이 절이 §12.4의 정량 근거이자 §11.3 표 '비용' 열의 참조원이다.

#### 12.6.1 제1원칙 — 비용은 '모드'로 분기한다 (솔로=0 / 경쟁=서버권위)

인프라 비용의 90%+ 유저(캐주얼)는 **한계비용 0**이어야 한다. 이를 아키텍처 규칙으로 못박는다.

| 모드 | 시세 권위 | 백엔드 부하 | per-user 한계비용 | 근거 정책 |
|---|---|---|---|---|
| **솔로/연습** (기본, 90%+) | 클라이언트 결정론 `f(seed,ticker,tickIndex)` | **0** (백엔드 미경유) | **$0** | MKT-01, 순위 미연동 |
| **주간 리그(비현금)** | 서버 권위 스냅샷(집계) | 스냅샷 평가만(WS 없음 가능) | 낮음(캐시 CPU) | GRF-02/04 |
| **실시간 대회** | 서버 권위 WS 팬아웃 | WS egress + 평가 | **동접에 비례** | MKT-01/07, GRF-11 |

- **규칙 IC-1(모드 게이팅):** 서버 권위 실시간 스트림은 **순위·보상이 걸린 경쟁 컨텍스트에만** 켠다. 솔로 홈·워치리스트·종목상세는 클라 결정론(시뮬) 또는 지연 캐시로 서비스하며 WS를 붙이지 않는다. → 실시간 인프라 청구가 되는 유저 = 전체 MAU가 아니라 **동시에 대회에 접속한 부분집합**뿐.

#### 12.6.2 실시간 전송(WS egress) 비용 — 동접별 산식

경쟁모드 실시간 전송비의 지배항은 **egress 대역폭**이다. 산식:

```
egress_bytes/s = U × V_eff × B × R_eff
  U      = 동시접속(경쟁모드) 유저 수
  V_eff  = 유저당 실제 푸시 심볼 수 (뷰포트 티어링 후)
  B      = 심볼 델타 페이로드 (~50B, 프레이밍 포함)
  R_eff  = 심볼당 유효 푸시 주기(Hz) — N초 집계로 상한
월 egress(GB) = egress_bytes/s × 2.59e6(s/월) / 1e9
월 egress비($) ≈ 월 egress(GB) × $0.09/GB (표준 클라우드 egress, 추정)
```

**캡 적용값(권고 운영점):** 뷰포트 티어링(가시 ~6종목 @0.5Hz + 보유 ~4종목 @0.2Hz) + N초 집계(N=2s) → **유저당 ≈ 200 B/s**.

| 동접(경쟁) | egress(캡 적용) | 월 egress | egress비/월 | 팬아웃·평가 컴퓨트 | 캐시·베이스라인 | **인프라 합계/월(추정)** |
|---|---|---|---|---|---|---|
| **5k** | 1.0 MB/s | 2.6 TB | ~$230 | ~$150 | ~$100 | **~$480** |
| **20k** | 4.0 MB/s | 10.4 TB | ~$930 | ~$400 | ~$200 | **~$1,530** |
| **50k** | 10 MB/s | 25.9 TB | ~$2,330 | ~$900 | ~$400 | **~$3,630** |

**캡 미적용(나이브: 보유+워치 ~30종목을 매 업스트림 틱 1Hz 그대로 푸시)** 시 유저당 ~1,500 B/s(7.5×) → **50k 동접 egress만 월 ~$17,500**. 즉 아래 캡들이 비용을 **7~8배** 절감하는 게 아니라 **폭증을 막는 상한선**이다.

- **캡 IC-2(N초 집계):** 서버는 업스트림 틱 속도와 무관하게 **심볼당 최대 1개 병합 스냅샷/ N초**만 팬아웃한다(N=2s 기본, 대회 막판 1s로 가변). → egress를 업스트림 틱 레이트에서 **분리**.
- **캡 IC-3(뷰포트 티어링):** 가시 종목 1~2s / 보유 5s / 나머지 30s·온디맨드, 뷰포트 밖은 고빈도 구독 해제(FlashList 가시성 연동, MKT-07). → egress를 심볼 유니버스 크기에서 **분리**.
- **캡 IC-4(관리형 실시간 회피 기준):** Ably/Pusher류 **동시연결·메시지 종량** 관리형 서비스는 5k 동접부터 자체호스팅 WS(Fargate/VPS + Redis pub/sub) 대비 급격히 비싸질 수 있음 → 5k 동접 초과 예상 시 자체호스팅으로 전환하고, 관리형은 5k 이하 MVP·대회 파일럿에만 사용.

#### 12.6.3 리더보드 스냅샷 평가 빈도 (컴퓨트 상한)

평가 컴퓨트는 **마크가격이 hot-cache에 있으면 CPU-only로 저렴**하다. 병목은 egress이지 평가가 아니다 — 단, 평가 빈도를 컨텍스트별로 티어링해 낭비를 막는다.

| 컨텍스트 | 평가 주기 | 방식 | 비고 |
|---|---|---|---|
| 실시간 대회(진행 중, 막판 1시간) | **10s** + 체결 이벤트 시 | 서버 재계산 | 순위 확정은 서버권위가만 사용(GRF-02) |
| 실시간 대회(그 외 구간) | 60s + 체결 이벤트 시 | 서버 재계산 | |
| 주간 리그 순위표 | **5min** + 체결 델타 | 근사 실시간이면 충분 | WS 불필요 |
| **비활성 계좌 지연계산** | **읽을 때만(lazy)** | 온디맨드 | 아래 캡 IC-5 |

- **캡 IC-5(비활성 지연계산):** 최근 7일 세션·미체결 대기주문·포지션 변동이 없는 계좌는 **주기 평가 대상에서 제외**하고 순위·NAV를 **조회 시점에만** 계산한다. → 대회 참가자 수가 아니라 **활성 참가자 수**에 평가비가 비례. 20k 참가 대회에서 활성 8종목 보유자 기준 10s 재계산 = 20k×8 = 160k 캐시 마크리드/10s(=16k/s) + 2만 스코어 정렬(<10ms) → **컴퓨트는 사실상 무료, egress가 유일한 스케일 변수**임을 확인.

#### 12.6.4 데이터 과금 곡선 — 2개 시나리오 + per-subscriber 트랩

데이터 비용은 **과금모델에 따라 MAU와의 관계가 근본적으로 다르다.**

**시나리오 A — 호출량 과금(Polygon/TwelveData 재배포 플랜):** 백엔드 팬인(동일 종목 다수 유저 = 업스트림 1콜, MKT-07)으로 **비용이 MAU와 분리**된다. 상한은 `distinct_hot_symbols ÷ cache_TTL`이며 유저 수와 무관.
- 예: 핫심볼 ~200 × 캐시 TTL 2~5s → 업스트림 40~100 calls/s 수준, 재배포 실시간 플랜(예: Polygon Advanced $199/월, 추정) **평탄 정액**. MAU가 10k→500k로 늘어도 데이터 청구는 그대로.

**시나리오 B — per-subscriber/per-user 과금(트랩):** 비용 = 단가 × 구독자(≈MAU). 무과금 90% 베이스에 치명적.
- 미국주식 **SIP 통합테이프(CTA/UTP) per-subscriber 피**가 대표 위험. 벤더가 거래소 non-professional 구독자 피를 **패스스루**하면 유저당 월 ~$1~3(디스플레이 non-pro, 추정), professional·depth는 수십 배. **150k MAU × $1.5 = 월 $225k** → §12.4 매출을 8배 초과, **영구 적자**.
- Polygon류 '평탄 재배포 플랜'이 안전한 이유가 바로 이것: 벤더가 거래소에 business-line 라이선스로 정산하고 우리는 **유저 수 무관 정액**을 낸다. 반대로 **거래소 직결/SIP 직접 구독/per-subscriber 패스스루 벤더**는 게이트에서 차단한다.

- **계약 게이트 IC-6(per-user 금지):** 무과금 소비자 베이스 대상 **실시간 주식 데이터 계약은 per-subscriber/per-user 과금이면 서명 금지.** 허용: (a) 호출량·정액 재배포 플랜, (b) 지연/일봉, (c) 크립토 거래소 WS($0). 계약서에 **캐싱 TTL ≥ 우리 푸시 주기(N초)** 명문화(MKT-07 캐시 정책과 충돌 방지), attribution·재배포 허용·해지 조항 확인 후에만 진행(MKT-06 체크리스트에 본 조항 추가).

#### 12.6.5 손익분기 유저 수 (호출량 모델 vs 트랩)

블렌디드 net ARPU **$0.116/MAU/월(기본 시나리오, §12.4)**, 동접(경쟁)을 MAU의 ~3%로 가정(추정).

| MAU | 동접(≈3%) | **A. 호출량모델 비용/월** | B. per-user 트랩 비용/월 | net 매출/월($0.116) | A COGS/매출 | 판정 |
|---|---|---|---|---|---|---|
| 10k | ~300 | 데이터$199 + 인프라~$300 ≈ **$500** | $15,000 | $1,160 | 43% | A: 흑자 / B: 적자 |
| 50k | ~1.5k | $199 + ~$480 ≈ **$680** | $75,000 | $5,800 | 12% | A: 흑자 / B: 적자 |
| **150k** | ~4.5k | $199 + ~$1,300 ≈ **$1,500** | $225,000 | $17,400 | 9% | A: 흑자 / **B: 대규모 적자** |
| 500k | ~15k | $199 + ~$3,600 ≈ **$3,800** | $750,000 | $58,000 | 7% | A: 흑자 / B: 적자 |

- **손익분기:** 호출량 모델에서 데이터+인프라 COGS는 **매출의 5~9%**로 수렴(수천~수만 MAU 부근에서 흑자 전환) → §12.4의 "관리 가능"은 **A 시나리오 한정 참**. per-user 트랩은 **어떤 MAU에서도 손익분기 없음**.
- **CCU 설계상한 정합(NFR-S02):** 설계 목표 CCU는 **단일값 10k**(NFR-S02)로 고정한다. 위 표의 **500k MAU × ~3% ≈ 15k 동접**은 이 10k 설계상한을 **초과**하므로, 500k MAU 규모는 **워커 수평확장·샤딩을 선행 통과한 뒤에만** 진입한다(용량초과 구간, §10.3.3 로드게이트 G1~G5). 즉 10k는 "무증설로 보장하는 상한"이고, 그 이상은 수평확장 경로(NFR-S01)로만 대응한다.
- 한국 실시간(KRX 정보사용료+재배포)은 별도 견적(연 수백만원대 가능성, 추정)으로 위 표 밖 → 국내장은 지연/일봉으로 시작(MKT-06 엣지케이스 정합).

#### 12.6.6 유닛 이코노믹스 편입 (신규 COGS 라인)

§12.4 유닛 이코노믹스에 **인프라·데이터 COGS/MAU** 라인을 신설한다.

- **인프라·데이터 COGS/MAU(추정):** 150k MAU 기준 월 ~$1,500 ÷ 150k = **~$0.010/MAU/월** (기본 net ARPU $0.116의 ~9%, IC-7 10% 게이트 이내).
- **게이트 규칙 IC-7:** 어떤 실데이터/실시간 인프라 결정도 **COGS/MAU가 블렌디드 ARPU의 10%를 초과하면 승인 게이트에서 반려**한다(광고 ARPU 재검토 리스크 §16 반영 시 임계 자동 하향). per-user 데이터는 이 게이트를 구조적으로 통과 불가 → 계약 단계(IC-6)에서 선차단.
- **핵심 정합:** §12.4 "진짜 고정비 리스크는 UA·백엔드 인프라"를 정량화하면 — UA는 여전히 최대 병목이나, **백엔드 인프라 리스크는 'MAU'가 아니라 '경쟁모드 동접'에 비례**하며(솔로=0), 캡 IC-1~5로 상한이 걸리고, **데이터 리스크는 과금모델 선택(IC-6)으로 이분법적으로 결정**된다. 즉 리스크는 '규모'가 아니라 **모드 분리·캡·계약 3개 레버**로 제어 가능하다.

---

## 13. 법무·컴플라이언스

### 13.1 4대 레드라인 (기능 기획 게이트)
| # | 금지 | 대체 |
|---|---|---|
| R1 | 개별 종목 시그널·개인 맞춤 매수 추천 제공 | 과거·일반 개념(비개인화)만, 무료 |
| R2 | 무료 API 시세 **재배포** | 재배포 허용 유료 라이선스 또는 시뮬 |
| R3 | 참가비+현금상금+랜덤워크 대회 | 무료·비현금 or AMOE |
| R4 | 매매 체결·실현/미실현 P&L 축하, loss-chasing 유도 연출·강박성 잦은 푸시(웰빙·사행 심사) | 축하는 P&L 독립 채널(순자산 마일스톤·평생 실력지표·좋은 판단 스트릭·배지/레벨·도감/세트·시즌/시나리오·공식 티어)에만 연결하고, 손실 후 즉시 재매수 유도·강박 푸시는 배제 |

**핵심 2차효과:** R1~R4 준수 = 가장 돈 되는 기능(유료 시그널·현금대회) 포기 → B2B·구독·코스메틱으로 매출 이전이 법무에서 파생.

### 13.2 면책 3단계 + 동의 원장
- (1)온보딩은 하단 미세 배너("모의투자·실제 성과 아님")만 노출하고, **전면 면책 동의(한/영, 체크박스+타임스탬프+버전 기록)는 소셜·결제·대회 진입 직전에 필수 게이트로 트리거**한다(§7.2와 동일한 단일 모델), (2)리더보드/수익률/공유카드 상시 마이크로 배너, (3)실데이터 시 "△분 지연·출처" 배지. **동의 원장 자체가 개인정보·공격표면**임을 인지해 최소·익명화 보관, 스크롤 강제·비다크패턴으로 실질 동의 확보.

### 13.3 자문업 경계·대회·개인정보
- **자문업 회피(가드레일 ③):** 개별 실시간 매수신호·개인 맞춤 매수 추천 배제. AI 도우미는 사실 서술·일반 원리까지만(개별 지시 출력 필터).
- **대회:** v1 비현금·무료. 현금 도입 시 AMOE·$600 IRS·$5,000 주별 등록·지오펜싱·비거주자 원천징수(W-8BEN)·**비현금 상금도 시장가치 과세** 검토·Official Rules.
- **개인정보:** 연령게이트(한14/EU16/미13), 초기 임계 미만 차단, **출생 연월만 수집**(데이터 최소화·아동 식별정보 축적 회피). 동의 분리, 데이터 주체 권리·계정삭제·국외이전 고지, ATT/광고동의.

### 13.4 앱스토어·추가 규제 (신규 식별)
- 바이너리 옵션 금지, Google Play Financial features declaration, 슬롯머신형 사행 UX 배제, OTA 심사우회 금지.
- **한국 게임물 등급분류(신규):** 확률형 아이템·시즌패스·리그 조합은 게임물관리위원회 등급분류 대상 가능성 → 한국 출시 전 법률검토 게이트, 회피 시 한국 빌드에서 확률형·시즌패스 제외 검토.
- **EU 접근성법(EAA, 2025.6 발효):** EU 배포 시 WCAG 2.1 AA를 **법적 릴리스 게이트**로 승격.
- **B2B FERPA/주별 학생 프라이버시법:** 파일럿도 DPA 선체결.
- **미국 주(州) 소비자 프라이버시법(LEG-13, US-first 필수):** CCPA/CPRA 등 20개+ 주법을 단일 "US 프라이버시 모듈"로 구현 — 수집시점 고지(Notice at Collection), Do Not Sell or Share(DNSS) 토글, 민감정보 사용제한(Limit SPI), **범용 옵트아웃 신호(GPC) 자동 수용**(CA·CO·CT·TX·OR·MT 강제), 미성년 판매/공유 옵트인. GDPR DSR 배관 재사용, 응답 SLA 45일.
- **제재·수출통제·지오블록(LEG-14):** OFAC 포괄제재 관할(쿠바·이란·북한·시리아·크림 등)을 가입·결제·앱스토어 배포에서 지오블록(다중신호 보수판정), B2B 상대방 SDN/Entity List 스크리닝, 암호기능 EAR 신고(앱스토어 암호화 질문 정직 응답, `ITSAppUsesNonExemptEncryption`). 릴리스 게이트로 운영.
- **EU DSA(LEG-06 UGC 확장):** 소셜/UGC 표면에 신고·이의제기·투명성 의무(DSA) 서브섹션 반영. 상세는 §8.5·use-cases UC-B29~B31.
- **UGC:** 실존 자산 매수 선동·시세조종성 게시 금지(크립토 실데이터 후 강화 모더레이션). 실제 상표·로고는 벤더 라이선스에 상표권 포함 확인, 미포함 시 텍스트만.
- **종목 유니버스 = 100% 가상 발행사 확정(오너 결정, living-market.md §6 LEG-16/17, High):** 살아있는 시장(회사특정 이벤트·속보 뉴스·스캔들·M&A 루머·증자)을 안전하게 제공하는 유일 경로는 가상기업이다. **실티커(AAPL 등) + 시뮬 가격 조합 자체가 실존 증권에 대한 "허위 시장정보" 노출면**(US SEC Rule 10b-5(b) / KR 자본시장법 §178 / JP 金商法 158) — 가짜 뉴스 이전의 문제이므로 실티커 유니버스는 이벤트 엔진 착수 이전에 **제거**한다(`src/data/stocks.ts`의 실명·실티커 12종은 이미 가상 발행사로 마이그레이션 완료). **CI "실티커 문자열 0" 릴리스 게이트** + 발행사 클리어런스 C1~C5(티커 denylist·JaroWinkler<0.85+Metaphone 음운유사·트레이드드레스·인물명·서술, LEG-16) + 법무 육안 스팟검수. 가상 티커는 실심볼과 시각적으로 구별(전용 접두 기호·상시 워터마크).
- **시뮬 라벨 상시 + No-AI 템플릿 뉴스(LEG-17):** 모든 시장데이터 표면(마켓 펄스 종목 랭킹·펄스 카드·속보 피드·`liveParticipants` 등)에 **'시뮬 데이터·시뮬레이션 참여자·추천 아님' 라벨을 상시** 렌더(FTC/표시광고법/景表法 기만 방어). 속보·뉴스는 **타입드 슬롯 통제어휘 치환의 No-AI 템플릿**만 사용(자유텍스트·실고유명사 주입 경로 구조적 부재), `templateId+legalReviewVersion` 스탬프·미승인 렌더 차단, 금지 7클래스(실존명·차별·실참사 연상·직접 매수매도 지시·수익보장/목표주가·추격 넛지·실시장 선동), 면책 3층(피드 배지+개별 워터마크+공유카드).
- **1000배 레버리지·인버스·파생 도박화 위험(신규, High):** 초고배율·인버스·옵션·선물·무기한은 도박 프레임으로 오인·앱스토어 사행성 심사 리젝 위험이 크다. **출시 조건(게이트):** ①진입 전 **원금 소진 가능성 사전 리스크 고지 게이트 + 2스텝 확인**(정본 LEV-08), ②"가상 자산·모의" 라벨 상시, ③**현금 상금 배제**(가드레일 ①), ④**레버리지·파생 성과의 고위험 리그 격리**(GRF-16), ⑤소셜 프로필 청산 이력 노출 강제(리스크 투명성, SOC-05). herding/시세유도는 실시간 고배율 포지션 기본 비공개·카피트레이드 미제공(SOC-06)으로 방어. 요소 추가 시 규제 카테고리 재평가를 기획 게이트에 포함.

---

## 14. 정책 요약

상세 규칙·엣지·사이드이펙트는 [policies.md](./policies.md) 참조. 오너 확정 신규 스코프(레버리지·파생·멀티마켓·소셜·살아있는 시장/NWS) 반영 후 현재 정책 카탈로그는 **16개 도메인·215개 정책**이며, **교육 도메인은 없다.** 신규·확장 정책의 상세 정책ID·번호는 [policies.md](./policies.md)를 정본으로 하고, 상품·시장·마진/청산은 [instruments-and-markets.md](./instruments-and-markets.md) §1, 소셜 표면은 [social-profiles.md](./social-profiles.md) §4~§5를 참조한다. 주요 정책군:

- **계좌·자산(ACC):** 시작자산 $100k 표준·고정분모, 리셋·파산회생(쿨다운·히스테리시스 — 해제선 25% / **파산회생 리필 = 일일 3%**), **일반(수시) 리필 = 일일 5%는 별개 파라미터**(§12.3), 외부현금흐름 원장 분리, 정수 센트, 원자적 체결·불변식, 다계좌·모드 격리, 스키마 마이그레이션, 튜토리얼 거래 격리(origin 태그), 리필 가드레일, 데이터 권리, 성과 기준선·실현손익 원장, 다통화.
- **주문·체결(ORD):** 주문유형 단계 도입(축과 마찰 분리), 입력검증·틱/랏사이즈, 원자성·멱등, 정수 정밀도, 체결가(mid/bid/ask)·슬리피지·수수료, 대기주문 엔진, TIF, OCO/브래킷/트레일링, 거부 코드·한국어 에러, 정정·취소·예약, 확인 스텝, 모드/대회 룰셋, 세션 게이팅, 코퍼레이트 액션 조정, Fill·원장 연계, 남용방지, 멱등·감사.
- **시세·시장(MKT):** 결정론 권위, 현실성 캘리브레이션, 영속·dayOpen, 세션, 출처·지연 고지, Provider·라이선스 게이트, 레이트리밋·캐싱·폴백, 이상치 격리, 서킷브레이커·정지, 정밀도·자산군, 코퍼레이트 액션, 대회 결정론 격리, 시나리오 주입, 체결 기준가·마찰.
- **포트폴리오·회계(ACC 회계):** 정수 최소단위·**round-half-even 단일 반올림**, 평균원가·수수료 자본화, 실현손익 원장, 미실현·mark, 총자산 정의, **수익률 이원화(고정분모/TWR)**, 외부현금흐름 분리, 마찰, 원자 정산·불변식, NAV 스냅샷(절대시각), 위험조정 지표, **배당=투자소득(numerator 포함)**, 원가법(평균/FIFO+워시세일 검토), 공매도·레버리지(margin interest 포함), 초기화·파산·다계좌, 스키마 마이그레이션.
- **게임화·랭킹 공정성(GRF):** 리스크조정 복합 스코어(절대수익 금지), 지표 계산 표준, 외부흐름 중립화, 서버 UTC 타임라인, 리그·승강·세그멘테이션, 하이롤러 격리, 투명성·프라이버시, 신규/비활성 보호, 시빌·봇·시드예측 방지, 멱등·감사, 제재·이의제기, 안티메트릭·서킷브레이커(시즌 중 개입은 신규진입·노출 축소로 한정).
- **수익화·과금(MON):** 무료/프리미엄 경계, 요금·체험, 광고 포맷·빈도, 리필 가드레일, IAP 카탈로그, 환불·자동갱신, 미성년 결제·부모동의, 엔타이틀먼트·영수증 검증, 사기·어뷰징 방지, 스토어 결제 준수, B2B 시트, pay-to-win 금지, 확률형 공시.
- **법무·컴플라이언스(LEG):** 제품 성격·레드라인 게이트, 3단계 면책·동의 원장, 자문업 경계, 대회·도박, 개인정보·연령, UGC 모더레이션, 게임화 UX 가드레일, ToS·가상자산 무가치성, 앱스토어 심사, AI 코치 경계, 관할·지오펜싱, 수익화 규제 접점, (신규)한국 게임물 등급분류·EAA·FERPA·상표권.
- **데이터·프라이버시·보안(DP):** 최소 수집·게스트 우선, 분류·보존, 로컬 암호화, 스키마 마이그레이션, 인증·세션, 전송·시크릿, 동기화·충돌, 서버 권한·RLS, 동의·추적, 삭제·이전, 국외이전, 아동 보호, 결제 보안, 로깅·침해 대응, 클라이언트 하드닝.
- **보안(SEC):** 감사로그 무결성, 계정탈취(ATO) 방어, 이상탐지, 웹 무결성, 직무분리.
- **접근성(A11Y):** WCAG 2.1 AA 기준선, 비시각 등가물(등락 삼중 인코딩·스크린리더), 접근성 릴리스 게이트(EU EAA 법적 게이트 포함).
- **국제화(I18N):** 로케일×통화 포맷, 번역 게이트, RTL 예방, 로케일 우선순위 단일 참조(§7.5).
- **운영·릴리스·QA(OPS):** 테스트·릴리스 게이트, CI/CD, SLO, 인시던트·재해복구(DR).
- **레버리지·마진·청산(LEV, 신규 #13, IP1):** 마진 계좌 4층 모델(Wallet–Margin–Equity), 개시/유지증거금·리스크 리밋 티어, 청산가·마진레벨·마진콜, 강제청산·부분청산·청산수수료, 손실 상한·보험기금·계좌 음수 방지(O13 확정), ADL(IP2 활성), 마진이자·자본비용, 레버리지 리그 격리·리스크 고지 게이트(LEV-01~08).
- **크립토(CRY, 신규 #14, IP4):** 24/7 거래·UTC 00:00 단일시계, 3층 가격(인덱스/마크/라스트)·고변동, 소수점 수량·최소명목·dust, 무기한 펀딩·마크가 청산·리스크리밋 티어, 24/7 리더보드·결정론 앵커 공정성, 김치프리미엄(CRY-01~06).
- **소셜·프로필·랭킹(SOC, 신규 #15, IP1):** 프로필 표면 표준, 공개범위·프라이버시 티어(전체/팔로워/비공개 × 필드단위), 비대칭 팔로우 그래프(순위 가중 0), 다차원 리더보드(GRF 위임), 성과 표시(리스크 투명·청산 이력), 카피/herding/시세유도 방지, 어뷰징·사칭·가짜팔로워, 미성년 안전, 크로스마켓·다통화 프로필(SOC-01~09).
- **확장 정책군(기존 도메인):** MKT 확장(옵션 프라이싱·그릭스·IV·무기한·선물·ETF/ETN·멀티거래소·세션·가격제한·FX, MKT-15~22), ORD 확장(옵션 4방향·공매도 로케이트·환전/auto-FX, ORD-24~26), ACT 확장(계좌유형 CASH/MARGIN 분기, ACT-15), ACC 확장(옵션 회계·3버킷·초고배율 청산·부분청산 원장·멀티월렛·ETF/ETN 회계, ACC-20~25), GRF 확장(레버리지·파생 리더보드 형평, GRF-16).

---

## 15. 유즈케이스 요약

상세 흐름·대안·예외는 [use-cases.md](./use-cases.md) 참조.

- **핵심 트레이딩·포트폴리오(UC-01~14):** 게스트 온보딩 첫 매수, 리셋, 시세 탐색, 종목 상세, 시장가 매수/매도(실현손익), 지정가·손절·익절, 대기주문 관리, 포트폴리오·손익, 거래내역, 워치리스트, 모드 전환, 데이터 지속·복원.
- **온보딩·게임화·소셜·수익화(UC-01~28):** 30초 온보딩+보장 배지, 연령게이트, 지연가입, 맥락형 팁, 레벨 진행·챌린지, 용어사전, 시나리오 도전, 데일리 미션, 스트릭·프리즈, 배지·트로피·수집, 레벨업, 주간 리그·승강, 하이롤러 리그(v2 조건부), 그룹 대회 개설·참가, 리스크조정 리더보드, 친구 추가, 공유 카드, 활동 피드, 구독 결제·체험·복원, 코스메틱/시즌패스 IAP, 리워드 광고, 파산 리셋, 면책·동의 관리, 계정 삭제·데이터 이전.

- **소셜·프로필 신규(UC-B32~B35, 정본 social-profiles.md §9):** 타 유저 프로필 열람(SOC-01/05), 프로필 공개범위·필드단위 프라이버시 설정(SOC-02), 팔로우/친구(비대칭 팔로우 vs 상호 친구, SOC-03), 다차원 리더보드 탐색(시장×상품×레버리지×기간×정렬, SOC-04). UC-B16~B19(소셜)·UC-B29~B31(신고·차단·모더레이션)과 상호참조.

**정합 확정 사항(유즈케이스 교차):** 온보딩 연령 수집은 '출생 연월', 전면 게이트는 결제·소셜 직전 지연; 지정가는 무료·무마찰, 프리미엄 게이트는 OCO/고급주문·마찰; MVP는 로컬 리더보드 미노출("개인 연습 기록"); 리셋은 대기주문 취소·예약 해제를 원자적으로 포함; 프로필 공개범위 기본값은 부분공개·서버 강제·미성년 하드 오버라이드(UC-B32~B35).

---

## 16. 리스크·사이드이펙트 대응

### 16.1 High 리스크
| 리스크 | 대응 |
|---|---|
| **Phase 0 회계 결함이 전 로드맵의 도미노 선행** (buy/sell 레이스·실현손익 부재) | 주1에 seed 주입+리듀서 원자화+실현손익 원장+정수센트를 'UX 변화 0' 최우선 착수, v1 소셜의 **하드 선행 게이트**로 못박아 병렬 착수 유혹 차단 |
| **MVP 리텐션 후크 공허화** (소셜·랭킹 서버 전 불가, P0 스트릭 비보상, 콘텐츠 소량) | 리텐션을 **게임 콘텐츠 진행감**(레벨 진행·시나리오 도전·배지 컬렉션)으로 전환, P0 스트릭은 로컬 코스메틱 보상까지 허용(순위 미연동), D7 목표는 콘텐츠 검증 후 확정 가설로 강등, 서버 도입 앞당김 재검토 |
| **온보딩 퍼널 붕괴** (게이트 누적으로 30초 실패) | 전면 게이트를 첫 매수 뒤로 지연, 각 스텝 독립 계측·A/B로 이탈 스텝 제거, 목표를 'time-to-first-trade'가 아니라 '게이트 통과율×첫매수율'로 |
| **한국 게임물 등급분류 미이행** (무등급 게임물 유통 = 불법·삭제·형사) | 한국 출시 전 게임위 등급 필요 여부 법률검토 게이트, 회피 시 한국 빌드에서 확률형·시즌패스·경쟁 리그 제외 |
| **크립토 실데이터 재배포 오판** (거래소 ToS 위반) | P1 착수 전 각 거래소 재배포 약관 개별 법무검토, 확인 전까지 크립토도 시뮬 유지 |
| **미성년 개인화·금융/도박 광고 노출** (COPPA/GDPR-K/학생법·리젝) | 광고 SDK 초기화를 연령판정·동의 이후로, 미성년 문맥광고만+금융/도박/코인 전역 차단, B2B 학생 광고 완전 제거 |
| **US 주(州) 프라이버시·제재 미준수** (CCPA/CPRA·GPC 미수용 집행, OFAC 제재국 서비스) | US 프라이버시 모듈(DNSS·GPC 자동수용·미성년 옵트인, LEG-13), OFAC 지오블록+SDN 스크리닝+EAR 신고(LEG-14)를 릴리스 게이트로 (§13.4) |
| **게임이 사행/게임물 카테고리로 이동** (재미 게임이 도박 프레임으로 오인·심사 리젝) | 확률형·loss-chasing 리필·절대수익 단일 하이롤러 등 사행 트리거 최소화, 요소 추가 시 규제 카테고리 재평가를 기획 게이트에 포함(재미는 유지하되 법적 가드레일 준수) |
| **1000배·인버스·파생 도박화** (초고배율·옵션·선물·무기한이 도박 프레임으로 오인·앱스토어 사행성 리젝) | **출시 조건으로 게이트화**(§13.4): 진입 전 원금소진 가능성 사전 리스크 고지 + 2스텝 확인(LEV-08), 가상 라벨 상시, 현금 상금 배제, 레버리지·파생 고위험 리그 격리(GRF-16), 프로필 청산 이력 노출 강제(SOC-05). herding·시세유도는 실시간 고배율 포지션 기본 비공개·원클릭 카피트레이드 미제공(SOC-06)으로 차단 |
| **실시간 고배율 포지션 브로드캐스트발 herding·시세유도** (팔로워 많은 계정의 1000배 실시간 노출이 모방·카피·시세조종 표면) | 실시간 포지션 기본 비공개, 공개해도 지연·요약·'추천 아님·모의 기록' 라벨, 팔로워 규모 비례 심사 강화, 안티메트릭(공개 직후 동일종목 매수 급증) 서킷브레이커(SOC-06·GRF-15) |
| **배당 이중 페널티** (외부흐름 제외+배당락으로 배당주 수익률 왜곡) | 배당을 투자소득으로 총수익률 numerator에 포함, 광고·미션 크레딧만 external 차감 |
| **반올림 규칙 3중 상충** (클라/서버 ±1센트 발산) | 단일 `Money` 값객체 + round-half-even, 순수 TS 공유, property test CI 게이트 |

### 16.2 Medium 리스크
| 리스크 | 대응 |
|---|---|
| **스트릭·프리즈 다크패턴 역류** (손실회피 수익화가 브랜드·심사 모순) | 프리즈 직접 유료판매 배제, 스트릭 압박 상한·회복 미션·장기 미접속 알림 중단, 웰빙 넛지를 변경 승인 게이트로 보호 |
| **게스트 로컬 데이터 유실** (고몰입 게스트 전소→부정 리뷰) | 첫 배지 직후 가벼운 백업(이메일만) 넛지 1회, 유실 가능성 설정·백업 맥락에서 고지 |
| **현지화 불일치** (한국어 우선으로 미국 UA 이탈) | 출시 시장 기준 1급 언어, 기기 로케일 자동 선택 |
| **접근성 처방 충돌** (2초 tick 라이브리전 낭독 폭탄) | 라이브리전은 액션 결과에만, 시세는 온디맨드 announce, 스크린리더 E2E 포함 |
| **B2B 부적합** (다크 고정·경쟁 게임화·미성년 웰빙) | 라이트/고대비 테마, 교사 게임화 on/off, 미성년 웰빙 완화를 파일럿 전 필수 요건 |
| **롱 레버리지 무이자** (공짜 레버리지가 도박 방치) | 차입원금×일리율 절대시각 일별 차감(숏 대차수수료와 대칭) |
| **시장충격 슬리피지 무발동** (현실 ADV 하 $100k 주문 impact≈0) | ADV 시뮬 스케일 또는 '주문/자기자본' 비율 함수로 재정의 |
| **평균회귀 시계열 샤프 왜곡** | 스냅샷 절대시각 고정, 시뮬 샤프 최소표본·장기구간 한정 |
| **크레딧 파밍이 파산·리필 가드레일 우회** (파산=raw equity, 순위=차감 비대칭) | 파산 판정도 external 차감 후 매매손익 equity로 통일 or 크레딧을 '비매매 지갑'에 분리 |
| **규제 마찰의 복리 효과** (면책·연령차단·비수익률 공유 누적이 K-factor를 성장 임계 미달로) | K-factor 앵커를 초대 루프로 이전, 규제 방어 정책들의 합산 성장 영향을 통합 계측 |

### 16.3 놓치기 쉬운 2차효과 (감사 요약)
- 하이롤러 격리 리그 존재 자체가 심사 리스크 → **출시 초기 스코프 제외**, 유저풀·평판 안정 후 옵트인·성인전용·비현금·강한 면책·심사노트 첨부.
- 위험조정 리더보드가 새 어뷰징(지표 게이밍)을 학습시킴 → 기간 일관성·규칙준수율 가중.
- 실데이터 신뢰 향상이 '실전처럼 느껴짐→실전 이식·자문 오인' 규제 노출 증가 역설 → 면책·"연습 전용" 프레이밍 유지.
- 다계좌×2초tick×종목수 성능 곱증가, 대회 리더보드는 비활성 계좌도 서버 스냅샷 요구(지연계산 상충) → 뷰포트 티어링+서버측 집계.
- 실패 불가 첫 매수가 매수를 trivialize(Robinhood 경계 지점) → '배움 축하' 프레이밍 + 직후 리스크 균형 카드.
- 배지 중첩(지연·모드·면책·출처)이 정보 위계 붕괴 → 배지 시각 예산 관리.

---

## 17. 로드맵·마일스톤

가정: 소규모 팀(개발 2~3, 디자인 1, PM 1). 기준일 2026-07-03.

> **P1 범위 SSOT:** build-ready 실행 범위의 정본은 [p1-scope.md](./p1-scope.md)이며, Phase 0 / MVP / P1-deferred 구분을 따른다. 이 §17은 캘린더 로드맵만 설명하며, `p1-scope.md`보다 넓은 상품·시장 아이디어를 같은 P1 실행 슬라이스로 승격하지 않는다.
>
> **상품 phasing 오버레이(§5.0 IP1~IP4):** Phase 0/MVP는 미국장 기반 **가상 US 주식**의 로컬 회계·결정론 엔진·30초 게스트 온보딩·로컬 진행감·시스템 상대/고스트 프리미티브 1개까지다. 스키마 예약은 가능하나, **풀 소셜 그래프·공개 프로필/팔로우/다차원 공개 리더보드·서버 권위 대회 인프라·고배율/고위험 거래 구현은 P1-deferred 이후**로 분리한다. 이후 **IP2**(옵션·선물·무기한·레버리지ETF/ETN)·**IP3**(한국·일본장·FX 멀티월렛)·**IP4**(코인 24/7)는 v1 후반~v2로 배치한다. 상세 property test 범위는 §19.4·정본 instruments §10을 따르되, Phase 0/MVP 게이트는 `p1-scope.md`의 acceptance checklist를 우선 적용한다.

### Q3 2026 — MVP: "혼자서도 돌아온다" (Phase 0/MVP — 로컬 회계·엔진)
- **목표:** D1 40% / D7 20%(가설), 튜토리얼 완주 60%, 스토어 심사 통과.
- **산출물:** Phase 0 회계 하드닝(**ACT-15 계좌유형 분기·ACC-21 3버킷 항등식 골격 예약 포함**, §6.1), 결정론 시세 엔진(**앵커 체크포인트 포함**, §10.3.2/§11.1), 30초 온보딩+D1 배지, 온보딩 미세 면책 배너+연령게이트(출생 연월)·개인정보, 레벨 진행 트랙+미션/챌린지 v0+선택적 팁 카드 24, 로컬 스트릭+프리즈(비순위), 기본 매수/매도·포트폴리오·거래내역·손익·reset, `MarketDataProvider`(시뮬)·`FxProvider`·`ExchangeSpec`(US 단일 관통) 예약·데이터모델 서버이관 대비, 소셜 그래프 없이 동작하는 시스템 상대/고스트/벤치마크 대회 프리미티브 1개.
- **마일스톤:** 8월 클로즈베타(TestFlight), 9월 소프트런치(미국 등 1개 지역, 영어 우선).
- **게이트:** Financial features declaration·시뮬 도박 UX 회피 사전 점검, `p1-scope.md` acceptance checklist 전부 PASS, **결정론 property test: 정수 센트 원장·round-half-even·CASH/MARGIN 불변식 분기·결정론 가격 리플레이·reset 회계 무손상 검증.**

### Q4 2026 — v1 파트1: P1-deferred 소셜·서버 파일럿
- **목표:** K-factor ≥ 0.3(초대 루프 기준), 주간 리텐션 개선.
- **산출물:** 계정/인증+백엔드 파일럿(형태·부하 게이트 O1 확정 후), 게스트→지연가입 마이그레이션, 다계좌 리팩터 검증, **커뮤니티 모더레이션(신고·차단·모더레이션 큐 — 소셜 공개 전 하드 선행 게이트, LEG-06)**, 제한된 그룹 비공개 대회+초대 실험, 서버 UTC 스트릭, **시즌 1 콘텐츠·배지 컬렉션 확장**. 공개 프로필·팔로우·다차원 공개 리더보드의 전면 출시는 `p1-scope.md`상 P1-deferred이며, 모더레이션·프라이버시·RLS 게이트 통과 후 별도 출시한다.
- **마일스톤:** 11월 소셜 베타, 12월 리그 시즌 1.
- **게이트:** RLS 100% 커버리지(프로필 필드단위 공개범위 서버 강제), 클라·서버 회계 발산 0(NFR-C02), 모더레이션 SLA·신고/차단 E2E, 공개 소셜 표면별 미성년·프라이버시 기본값 검증.

### Q1 2027 — v1 파트2: 수익화 + IP2 파생 착수
- **목표:** 인스톨→유료 2%+, 광고 ARPU 기준선(재모델 후).
- **산출물:** Plus 구독+7일체험+소프트 페이월, `useEntitlement()`, 리워드 광고(상한/쿨다운), 코스메틱 IAP, 리얼/쉬운 모드+모드별 리그, **시즌패스·수집 확장**. **IP2 파생 착수: 옵션(블랙숄즈·그릭스·IV, 유럽식 CASH_SETTLE)·선물(CME ES/MES/MNQ)·무기한(펀딩)·레버리지/인버스 ETF·ETN(MKT-15~18·ORD-24·ACC-20), LEV-06 ADL·교차마진 활성, SOC-04 옵션/선물 리그 축 개방.**
- **마일스톤:** 2월 수익화 A/B(가격·체험·페이월 배치), 3월 결제 지역 확대, 3월 옵션/선물 클로즈베타.
- **게이트:** 결제 무결성(NFR-SEC05), **결정론 property test: 펀딩 Σ롱+Σ숏=0·옵션 만기·arbitrage-free IV(총분산 T 단조↑)·레버리지ETF 다중일 NAV=Π(1+L·r)−드래그(경로의존).**

### Q2 2027 — v2: B2B + 실데이터 심화 + IP3/IP4 멀티마켓·코인
- **목표:** B2B 파일럿 3~5개 기관, ARPU 상승(상위 티어).
- **산출물:** 그룹/기관 콘솔(웹, 라이트 테마), 그룹 대회·커스텀 규칙·대시보드, 실(지연)데이터 승격, Pro/Max 티어, 코퍼레이트 액션, AI 도우미(선택적 팁·요약, 개인 맞춤 매수 추천 금지, 법무검토 후). **IP3 한국·일본장 + FX 멀티월렛(MKT-19~22·ORD-26·ACC-24, SOC-09 크로스마켓 프로필), IP4 코인 24/7 현물+무기한(CRY-01~06, IP2 파생 엔진 재사용).** 공매도·레버리지 강제청산·margin interest는 IP1에서 이미 확정(O13 해결, §18) — v2는 멀티마켓·코인 배선.
- **마일스톤:** 5월 B2B 파일럿 계약(DPA 선체결), 6월 실데이터 정식 전환, 6월 KR/JP·코인 베타.
- **게이트:** 한국 게임물 등급분류 법률검토(§13.4), **결정론 property test: 다통화 base 항등식 ±1최소단위·FX 삼각환산(KRW→JPY=USD 경유)·DST 전환일 1시간 오차 0·크립토 24/7 앵커 결정론(48h 콜드스타트 <100ms).**

---

## 18. 오픈 이슈·의사결정 필요 사항

| # | 이슈 | 결정 필요 시점 | 후보/방향 |
|---|---|---|---|
| O1 | **서버 도입 '시점 + 형태 + 부하'** — (a)언제 당길지 (b)관리형(Supabase 데이터평면)만인지 **상시 워커+Redis(실시간평면)를 포함**하는지 (c)어느 대회 부하(N×30×M 틱/초)까지 검증하고 진입할지 | MVP 설계 확정 전 + 소셜 마일스톤 게이트 | 관리형 단독은 실시간 팬아웃 호스팅 불가(§10.3.1) → **상시 워커 형태와 로드게이트(§10.3.3 G1~G5)를 O1에 병합 결정**. 콘텐츠 진행감으로 우선 방어 vs 서버 조기 도입 |
| O2 | **광고 ARPU 재무모델 재추정** — 가드레일·비개인화 반영 시 광고 매출 하방 | Q1 2027 수익화 전 | 구독·B2B 중심 기본안 확정 |
| O3 | **웹(Expo Web) 저장소 폴백 검증** — MMKV 웹 미지원, expo-sqlite 웹 검증 | Phase 0 | StorageProvider 추상화 구현 확정 |
| O4 | **한국 게임물 등급분류 필요 여부** | 한국 출시 전 | 법률검토 결과에 따라 한국 빌드 기능 분기 |
| O5 | **크립토 거래소 재배포 약관** | P1 전 | 개별 법무검토, 미확인 시 시뮬 유지 |
| O6 | **소액 시작자산 모드의 리그 배정** | v1 리그 설계 | 별도 리그 분리 vs 정규화 |
| O7 | **장기 미접속 후 결정론 재계산 갭 처리** — 수개월치 GTC·손절 대량 갭 체결 순서·통지 | v1 대기주문 엔진 | 캐치업 평가 규칙·유저 통지 명세 |
| O8 | **오프라인 리필/크레딧 매매의 서버 롤백** — 무효화 가상현금으로 이미 체결한 포지션 정산 | v1 동기화 | 비매매 지갑 분리 or 롤백 UX 명세 |
| O9 | **P0 로컬 스트릭 마일스톤 배지 지급 여부** — 시각 조작 파밍 vs 후크 | MVP | 서버 검증 전 보류 vs 코스메틱만 |
| O10 | **하이롤러 리그 도입 시점·조건** | v2 이후 | 유저풀·평판·심사 리스크 재평가 |
| O11 | **B2B 학생 개인 삭제권 vs 학교 교육기록 보존 우선순위** — 컨트롤러 결정 규칙 | B2B 파일럿 전 | FERPA/PIPA 기반 계약 명문화 |
| O12 | **확률형 코스메틱 채택 여부** — 게임물 등급·EU 루트박스 규제 vs 매출 | v1 IAP 설계 | 확정 코스메틱·시즌패스 중심으로 회피 검토 |
| O13 | **강제청산 원금초과손실 시 equity 음수 화해** — 파산 임계·cash≥0 불변식과의 단일 규칙 | ✅ **해결됨**(정본 instruments §4.5·§7) | **LEV-05**(격리 손실 상한 = 구속 증거금 M0 이내 + 시뮬 **보험기금**이 파산가 관통 잔손실 흡수, 지갑 불변) + **ACC-22**(음의 `accountEquity`를 0으로 clamp, 초과분은 `INSURANCE_ABSORB` type으로 유저 원장 **밖** `insuranceFundAbsorbedCents` 별도 카운터에 상각 → 포지션당 최대손실 정확히 −M0, equity≥0 항등식 유지, 보험기금은 유저 equity 구성요소 아님). ADL(LEV-06)은 IP2 활성 |
| O14 | **운영·릴리스 게이트 통합(OPS, policies 도메인 12)** — §13.4·§16·§17·§19에 흩어진 릴리스 게이트를 단일 체크리스트로 수렴, SLO·인시던트·DR 오너십 | v1 백엔드 전 | OPS 도메인 기반 단일 릴리스 게이트로 수렴(§19.7) |
| O15 | **국제화 로케일별 법적문구·번역 게이트(I18N, policies 도메인 11)** — 로케일×통화 포맷·RTL 예방·로케일별 면책 문구 | 다지역 확장 전 | §7.5 로케일 우선순위 단일참조 + I18N 정책 |


---

## 19. 비기능 요구·릴리스 기준

> **원칙:** NFR은 "빠르면 좋다"가 아니라 **숫자·측정도구·판정주체·릴리스 연결**을 가진 계약이다. 각 항목은 (1)정량 목표, (2)측정 방법·표본, (3)위반 시 조치(릴리스 차단/경보/부채등록)를 명시한다. **G=하드 게이트(미달 시 해당 단계 릴리스 차단)**, **W=경보(부채 등록·다음 스프린트 시정)**. 측정은 자동화(CI·대시보드) 우선, 수동 QA는 보조. 릴리스 게이트 원칙과 단계별 종료 기준(Exit Criteria)은 §19.7~19.8에 함께 둔다.

### 19.1 성능 (Performance)

| ID | 목표(수치) | 측정 방법 / 표본 | 도입 단계 | 게이트 |
|---|---|---|---|---|
| NFR-P01 | **콜드 스타트 < 2.5s**(p90, 스플래시→홈 인터랙션 가능), 중저가 기준기(iPhone SE2·Pixel 6a) | EAS 릴리스 빌드 계측(`expo-performance`/`react-native-performance` TTI 마크), 릴리스마다 20회 측정 p90 | MVP | **G** |
| NFR-P02 | **웜 스타트 < 1.0s**(p90) | 동일 계측, 백그라운드 30s 후 복귀 | MVP | W |
| NFR-P03 | **마켓/포트폴리오 스크롤 60fps**(2초 tick 갱신 중), dropped-frame < 1%(FlashList 가시행) | Flashlight/`react-native-performance` frame timing, 12·50·200종목 3케이스 30s 스크롤 | MVP | **G**(12종목), W(200종목) |
| NFR-P04 | **매매 입력→체결 토스트 < 150ms**(p95, 로컬 결정론 경로) | domain 레이어 벤치 + E2E(Maestro) 타임스탬프 | MVP | **G** |
| NFR-P05 | **2초 tick 리렌더 폭풍 억제:** tick당 리렌더 컴포넌트 수 ≤ 가시/보유 종목 카드 수(전역 재렌더 0) | React DevTools Profiler·렌더 카운터 테스트(Zustand selector 구독 검증) | MVP | **G** |
| NFR-P06 | **리더보드 조회 API p95 < 400ms**, p99 < 800ms(30명 리그·상위100 페이지) | k6/Artillery 부하 시나리오, Supabase Edge Function·SQL 집계 뷰 계측 | v1 | **G** |
| NFR-P07 | **대회 실시간 순위 반영 지연 < 3s**(체결→리더보드 반영, p95) | 서버 이벤트 타임스탬프 대시보드(상시 워커 WS 팬아웃, §10.3.1 실시간평면 — Supabase Realtime 미사용) | v1 | **G** |
| NFR-P08 | **번들/OTA:** 초기 JS 번들 < 4MB(gzip), OTA 업데이트 페이로드 < 2MB | EAS Update 산출물 검사 CI 스텝 | MVP | W |
| NFR-P09 | **웹(Expo Web) LCP < 3.0s**, INP < 200ms(소프트런치 랜딩·공유카드 딥링크) | Lighthouse CI, 딥링크 URL 3종 | v1 | W |

**근거·2차효과:** NFR-P03/P05는 §10.1 "2초 tick 재렌더 폭풍"·§16.3 "다계좌×tick×종목수 성능 곱증가"의 정량 방어선이다. tick 주기(2s)를 성능 예산 변수로 노출해, 미달 시 자산군별 tick 감쇠(비가시 종목 저빈도 구독)로 시정한다. 콜드스타트 목표를 중저가 기준기로 못박는 이유: 고사양 기기에서만 통과하면 실제 신규 유저 이탈(온보딩 §16 퍼널)을 못 잡는다.

### 19.2 안정성·가용성 (Reliability / Availability)

| ID | 목표(수치) | 측정 방법 | 도입 단계 | 게이트 |
|---|---|---|---|---|
| NFR-R01 | **Crash-free 세션 ≥ 99.5%**, Crash-free 유저 ≥ 99.0%(7일 롤링) | Sentry(또는 동급) 릴리스 헬스, 소프트런치 최소 표본 1,000세션 | MVP | **G** |
| NFR-R02 | **회계 불변식 위반(assert) 프로덕션 발생률 = 0**(I1~I10, §19.4) | 런타임 assert→원격 로깅(ORD-반영·ACC-05), 위반 1건도 P0 인시던트 | MVP | **G**(0-tolerance) |
| NFR-R03 | **데이터 유실 0:** 앱 강제종료/OTA 갱신/스키마 마이그레이션 통과 후 원장 무손실 | expo-sqlite WAL·아웃박스 crash 주입 테스트, 마이그레이션 라운드트립(I10) | MVP | **G** |
| NFR-R04 | **대회 마감 판정 SLO 99.9%**(시즌·대회 종료 시각에 서버 권위 스냅샷·정산이 ±60s 내 정확 수행) | 서버 크론·Edge Function 성공률 대시보드, 시즌 종료 이벤트 감사로그 | v1 | **G** |
| NFR-R05 | **백엔드 월 가용성 ≥ 99.5%**(인증·리더보드·대회 API), 계획 점검 제외 | Supabase 상태·합성 모니터(uptime probe 1분 간격) | v1 | W→G(수익화 후) |
| NFR-R06 | **시세 프로바이더 장애 시 폴백 < 5s**(실데이터 타임아웃→시뮬 폴백, 서킷브레이커) | 프로바이더 장애 주입 테스트, `Quote.source` 배지 전환 검증 | v2(실데이터) | **G** |
| NFR-R07 | **오프라인 완전 작동:** 네트워크 0에서 솔로 매매·미션·스트릭 무결, 복귀 시 아웃박스 동기화 성공률 ≥ 99.9% | 기내모드 E2E, 동기화 충돌 시나리오(O8) | v1 | **G** |

**근거:** NFR-R04(대회 마감 99.9%)는 "실력 콘테스트" 공정성(§8.6·GRF)의 정량 계약 — 마감 시각 정산이 흔들리면 리그 신뢰가 붕괴한다. NFR-R02는 "돈 계산 무결성"을 **0 허용치**로 격상해 §6.1 Phase 0 결함의 재발을 릴리스 레벨에서 봉쇄한다.

### 19.3 확장성·용량 (Scalability / Capacity)

| ID | 목표(수치·상한) | 측정 방법 | 도입 단계 | 게이트 |
|---|---|---|---|---|
| NFR-S01 | **MAU 설계 상한:** v1 100k MAU까지 인프라 무증설 통과(부하테스트 근거), 200k까지 수평확장 경로 문서화 | k6 램프업(가상 1k→10k 동접), Supabase 커넥션풀·읽기복제 용량 모델 | v1 | W(테스트)·G(경로 문서) |
| NFR-S02 | **동시접속(CCU) 설계상한(단일값) = 10k:** 워커 WS 팬아웃 채널 동접 10k까지 무증설로 NFR-P06/P07 유지. 초과분(예: 500k MAU×3%≈15k, §12.6.5)은 수평확장(NFR-S01) 선행 후에만 진입 | 워커 WS 채널 부하테스트, 팬아웃 시나리오 | v1 | W |
| NFR-S03 | **원장 성장 상한·성능:** 계좌당 거래 100k건에서 포트폴리오 계산 < 500ms, 시계열 스냅샷 쿼리 < 300ms | expo-sqlite 대용량 시드 벤치, 인덱스 검증 | MVP(로컬)·v1(서버) | **G** |
| NFR-S04 | **대회 규모:** 단일 대회 참가자 5,000명까지 결정론 시세·순위 집계가 SLO(NFR-R04) 내 | 서버 집계뷰 부하테스트, 시드 재생 성능 | v2 | W |
| NFR-S05 | **다계좌×tick 곱증가 억제:** 계좌 5개·보유 200종목에서 NFR-P03/P05 유지(뷰포트 티어링·서버측 집계) | 최악 케이스 성능 프로파일(§16.3) | v1 | **G** |

**근거:** §12.4 "$30k net MRR 매출 마일스톤(net 기본 ~259k MAU)" 유닛이코노믹스를 인프라 관점에서 뒷받침 — 상한을 못박아야 "규모가 수익화보다 선행 병목"이라는 판단이 용량 계획으로 실행된다. 원장 100k건 상한(NFR-S03)은 파산·리셋 아카이브(ACC)·이벤트소싱 append-only 설계의 성장 한계를 명시한다.

### 19.4 정확성·결정론 (Correctness / Determinism)

| ID | 목표 | 측정 방법 | 도입 단계 | 게이트 |
|---|---|---|---|---|
| NFR-C01 | **회계 불변식 I1~I10 100% 통과**(fast-check property 스위트, 각 ≥ 1,000 케이스) | CI 필수 잡, 실패 시 머지·릴리스 차단 | MVP | **G** |
| NFR-C02 | **클라·서버 회계 발산 = 0**(동일 이벤트열 → ±0센트, `Money` 값객체·round-half-even 공유) | 순수 TS domain 레이어 골든 벡터 테스트(양 런타임 동일 입력) | v1 | **G** |
| NFR-C03 | **시세 결정론 재현:** 동일 `(seed,ticker,tickIndex)`가 전 기기·재기동·재계산에서 비트동일 | 결정론 스냅샷 테스트, 대회 서버 시드 리플레이(해시체인 감사) | MVP(로컬)·v1(대회) | **G** |
| NFR-C04 | **대기주문 평가 결정론:** 동일 틱 다수 주문의 발동·우선순위·부분체결이 재현(ORD-08) | evaluatePendingOrders 리플레이 테스트, 갭 캐치업(O7) 케이스 | v1 | **G** |
| NFR-C05 | **마이그레이션 무손실(I10):** 모든 `schemaVersion` 업그레이드가 round-trip 무손실, 실패 시 백업 키 격리(자산 유실 0) | 버전쌍별 마이그레이션 테스트 + 실기기 업그레이드 리허설 | MVP | **G** |
| NFR-C06 | **신규 스코프 결정론 property test(정본 instruments §10):** 범위를 확장한다 — ①`leverage∈[1,1000]` 전 구간 청산가·마진레벨·개시증거금 정합, ②**부분청산 N단계** 텔레스코핑 `ΣVM + 최종청산손익 == (exit−entry)×qty×mult`, ③**옵션 만기**(ITM/OTM·행사·배정) 손익 정합·arbitrage-free IV(총분산 T 단조↑·나비 비음수), ④**다통화 base 항등식 ±1최소단위**(원통화 정수→fx 1회 곱→base round-half-even), ⑤**펀딩 Σ롱+Σ숏=0**(동일시각 제로섬)·숏배당=롱배당, ⑥**FX 삼각환산**(KRW→JPY = USD 경유 1회 곱). **청산·펀딩·정산가·SQ·FX·마진이자는 전부 서버 UTC 절대시각**(앱-오픈 빈도·타임존 무관, 대회 결정론·리더보드 공정성 전제) | fast-check property 스위트(각 ≥ 1,000 케이스) + 대회 서버 시드 리플레이(해시체인 감사) | IP1(①②)·IP2(③⑤)·IP3(④⑥) | **G** |

**I1~I10 불변식 스위트 (fast-check, ACC-05/ACC-09 근거):**

> **마진 계좌 확장 불변식(정본 §7.3, IP1부터 분기 적용):** MARGIN 계좌에선 I1(`cash≥0`)이 폐기되고 **IM1** `walletBalance≥0` · **IM2** `isolatedMargin≥0` · **IM3** `positionEquity≥0`(격리) · **IM4** `accountEquity≥0`(계좌 음수 절대 불가) · **IM5** `Σ격리 실현청산손실 ≤ Σ구속증거금`(포지션당 최대손실 정확히 −M0, O13) · **IM6** walletBalance=원장 합 일치로 분기된다. 아래 I1은 CASH 계좌 전제이며 계좌유형(ACT-15)에 따라 IM 세트로 대체된다.

| ID | 불변식 | 위반 = |
|---|---|---|
| I1 | `cashCents ≥ 0`(레버리지 계좌 제외) | 현금 음수(더블탭 레이스) |
| I2 | 모든 `holding.qty > 0`(0이면 키 제거) | 초과매도·유령 포지션 |
| I3 | `costBasisCents ≥ 0`(가치보존) | 원가 붕괴 |
| I4 | `totalValue == cash + Σ(mark×qty)` | 평가액 불일치 |
| I5 | `accountDelta == tradingPL + netExternalFlows`(±최소단위) | 외부흐름 오염 |
| I6 | 저장→로드 round-trip 동일 | 영속화 손실 |
| I7 | 전량매도 후 holdings 키 제거 + realizedPL 원장 누적 유지 | 청산 손익 소실 |
| I8 | 정수 센트·round-half-even 단일 규칙(부동소수 누적오차 0) | ±1센트 튐 |
| I9 | `totalValue − startingEquity == realizedPL + unrealizedPL` | 손익 정합 붕괴 |
| I10 | 마이그레이션 round-trip 무손실(schemaVersion) | 업그레이드 자산 유실 |

### 19.5 보안·프라이버시 (Security / Privacy)

| ID | 목표 | 측정 방법 | 도입 단계 | 게이트 |
|---|---|---|---|---|
| NFR-SEC01 | **인증:** 세션 토큰 만료·회전 정책 준수, 리프레시 토큰 안전저장(SecureStore/Keychain), 무차별 대입 레이트리밋 | 인증 플로우 보안 테스트, 토큰 저장 정적검사 | v1 | **G** |
| NFR-SEC02 | **RLS 100% 커버리지:** 모든 사용자데이터 테이블에 행수준 보안, "타 유저 데이터 0 접근" 회귀 테스트 | RLS 정책 스냅샷 테스트(음성/양성 케이스), 스키마 CI 린트 | v1 | **G** |
| NFR-SEC03 | **치팅 방지(대회 무결성):** 서버 사이드 이벤트 검증 로그·서버 UTC 시계·은닉 시드로 (a)시각조작 스트릭, (b)미래가 예측, (c)시빌/봇 차단. 탐지된 어뷰징 이벤트 오탐률 목표·제재 파이프라인 정의(GRF) | 어뷰징 시뮬레이션 스위트(시각조작·시드추정·중복계정), 탐지율 대시보드 | v1 | **G** |
| NFR-SEC04 | **API 키 은닉:** 시세/결제 키 클라이언트 미노출(100% 백엔드 프록시), 시크릿 스캔 CI | 번들 시크릿 스캐너(gitleaks/trufflehog) CI 잡 | MVP | **G** |
| NFR-SEC05 | **결제 검증:** 구독/IAP 서버 영수증 검증(RevenueCat 웹훅), 엔타이틀먼트 위·변조 차단 | 위조 영수증 리젝 테스트 | v1 | **G** |
| NFR-SEC06 | **개인정보 최소화·권리:** 출생 연월만 수집, 계정삭제 SLA ≤ 30일, 데이터 이전(export) 제공, 동의 원장 최소·익명 보관 | DSR(데이터주체요청) E2E, 수집 필드 감사(§13.3) | MVP(로컬)·v1(서버) | **G** |
| NFR-SEC07 | **광고 SDK 안전초기화:** 연령판정·동의 이후에만 초기화, 미성년·B2B 학생 문맥 금융/도박/코인 광고 전역 차단 + 비개인화 | SDK 초기화 순서 테스트, 카테고리 차단 검증(§16.1) | v1 | **G** |
| NFR-SEC08 | **전송·저장 보안:** 전 구간 TLS, 로컬 민감데이터 암호화, 클라이언트 하드닝(루팅·디버깅 탐지 수준 정의) | 정적/동적 보안 점검, 침해대응 로깅 | v1 | W→G(수익화 후) |

**근거:** NFR-SEC02/SEC03은 §8.6·GRF의 "치팅 방지 서버 검증"과 §11.1 "은닉 시드"를 릴리스 판정 가능한 형태로 계약화한다. RLS를 100% 커버리지·회귀테스트로 못박는 이유: 한 테이블만 누락돼도 리더보드·대회 데이터 경계(§10.1 Supabase 결정 근거)가 무너진다.

### 19.6 접근성·현지화 (Accessibility)

| ID | 목표 | 측정 방법 | 도입 단계 | 게이트 |
|---|---|---|---|---|
| NFR-A01 | **WCAG 2.1 AA 준수**(대비 4.5:1, 등락 삼중 인코딩 색+화살표+부호, 터치타깃 ≥ 44pt) | axe-core/Accessibility Scanner 자동검사 + 수동 체크리스트, 위반 0(AA) | MVP | **G**(핵심 플로우) |
| NFR-A02 | **EU 배포 시 WCAG 2.1 AA = 법적 릴리스 게이트(EAA 2025.6)** | EU 빌드 전 접근성 감사 리포트 필수 첨부 | v1(EU) | **G**(법적) |
| NFR-A03 | **스크린리더 무결:** 라이브리전은 액션 결과(체결·에러)에만 assertive, 2초 tick은 `aria-live=off`+온디맨드 announce(낭독 폭탄 0) | VoiceOver/TalkBack E2E 시나리오(§16.2) | MVP | **G** |
| NFR-A04 | **라이트/고대비 테마 최소 1종**(B2B 파일럿 전), 시맨틱 토큰 100%(하드코딩 색상 0) | 테마 스냅샷 테스트, `theme.ts` 토큰 린트 | v2(B2B 전) | **G** |

### 19.7 릴리스 게이트 원칙 + Phase 0 차단 게이트

> **원칙:** 각 마일스톤은 아래 **Exit Criteria 표(§19.8)를 100% 충족(모든 G 항목 PASS)해야 다음 단계로 진입/출시**한다. Exit Criteria는 "했다"가 아니라 **측정치·판정주체·증빙(대시보드/CI 링크)**을 요구하며, PM이 릴리스 회의에서 항목별 PASS/FAIL을 기록한다. FAIL 1건이라도 있으면 릴리스 차단(예외는 O-이슈로 등록 후 서면 승인).

#### 19.7.1 Phase 0 완료 = 하드 릴리스 차단 게이트 (모든 이후 단계의 선행)

Phase 0(§6.1 회계·엔진 하드닝)은 **UX 변화 0의 정확성 계약**이며, **미완료 시 MVP를 포함한 어떤 빌드도 스토어·베타로 내보내지 않는다.** "병렬 착수 유혹 차단"(§16.1)을 게이트로 강제한다.

| # | Phase 0 종료 기준(측정가능) | 판정 |
|---|---|---|
| PH0-1 | I1~I10 fast-check 스위트 **100% 통과**(각 ≥ 1,000 케이스), CI 필수 잡 그린 | NFR-C01 · **G** |
| PH0-2 | 실현손익 원장 정상 — 전량청산 시 realizedPL 0 표시 버그 **재현 불가**(회귀 테스트 포함) | I7·I9 · **G** |
| PH0-3 | buy/sell 원자화 — 더블탭 200ms×2 부하에서 cash 음수·초과매도 **재현율 0**(1,000회 퍼즈) | I1·I2 · **G** |
| PH0-4 | 정수 센트 회계 — round-half-even 단일 규칙, 부동소수 누적오차 **0**(property test) | I8 · **G** |
| PH0-5 | 시세 결정론 — 동일 `(seed,ticker,tickIndex)` 재기동·재계산 **비트동일**, 세션 간 영속 | NFR-C03 · **G** |
| PH0-6 | 변동성 정규화 — 연환산 σ가 실현목표에 캘리브레이션(§11.2 내부모순 회피), 무제약 확산 없음 | **G** |
| PH0-7 | 마이그레이션 무손실 — `schemaVersion` 라운드트립 무손실, 실패 시 백업 키 격리(자산 유실 0) | I10·NFR-C05 · **G** |
| PH0-8 | 외부현금흐름 분리 — 광고·미션 크레딧이 `externalFlows` 원장 경유(cash 직접 가산 0), 성과 분모 정합 | I5 · **G** |

**게이트 강제 메커니즘:** 위 8개 항목은 CI 파이프라인의 **required check**로 등록해 코드레벨에서 미충족 머지를 차단한다. PH0 전 항목 PASS 증빙(CI 링크) 없이는 EAS 릴리스 프로파일 빌드를 승인하지 않는다.

### 19.8 단계별 Exit Criteria

> 각 분기 마일스톤(§17) 진입/출시의 종료 기준이다. G 항목은 전부 PASS여야 다음 단계로 넘어간다.

**Q3 2026 — MVP Exit Criteria**

| 영역 | 종료 기준(측정가능) | 게이트 |
|---|---|---|
| 정확성 | Phase 0 게이트 8종(§19.7.1) 전부 PASS | **G** |
| 안정성 | Crash-free 세션 ≥ 99.5%·유저 ≥ 99.0%(클로즈베타 ≥ 1,000세션), 불변식 assert 프로덕션 0 | **G**(NFR-R01/R02) |
| 성능 | 콜드스타트 p90 < 2.5s(중저가 기준기), 12종목 60fps, 매매→토스트 p95 < 150ms | **G**(NFR-P01/P03/P04) |
| 온보딩 | **게이트 통과율 × 첫 매수 완료율 ≥ 70%**(각 스텝 독립 계측·A/B), 튜토리얼 완주 ≥ 60% | **G**(§4.2) |
| 접근성 | 핵심 플로우 WCAG AA 위반 0, 스크린리더 체결/에러 E2E PASS, 시세 낭독폭탄 0 | **G**(NFR-A01/A03) |
| 데이터 무손실 | 게스트→서버 마이그레이션 라운드트립 선검증, 강제종료 후 원장 무손실 | **G**(NFR-R03) |
| 규제 | Financial features declaration 사전점검, 시뮬 도박 UX 회피 체크리스트, 면책·연령게이트(출생 연월)·에러 한국어 3원칙 검증 | **G**(§13) |
| 콘텐츠 | 선택적 팁 카드 24·미션/챌린지 v0·시나리오 6·레벨 진행 트랙 활성, 로컬 스트릭+프리즈(비순위) 동작 | W |

**Q4 2026 — v1 파트1(소셜 코어) Exit Criteria**

| 영역 | 종료 기준(측정가능) | 게이트 |
|---|---|---|
| 결정론·공정성 | 클라·서버 회계 발산 0(NFR-C02), 대회 서버 시드 리플레이 해시체인 감사 PASS(NFR-C03) | **G** |
| 대회 SLO | 대회/시즌 마감 판정 SLO 99.9%(±60s 정산), 실시간 순위 반영 p95 < 3s | **G**(NFR-R04/P07) |
| 성능 | 리더보드 API p95 < 400ms(부하테스트), 다계좌 5×200종목에서 60fps 유지 | **G**(NFR-P06/S05) |
| 보안 | RLS 100% 커버리지 회귀 PASS, 인증 토큰 정책, 치팅방지(시각조작·시드추정·시빌) 시뮬 스위트 PASS | **G**(NFR-SEC01~03) |
| 모더레이션 | 신고·차단·모더레이션 파이프라인 가동(소셜 공개 전 선행 게이트), 어뷰징 리포트 처리 SLA 정의(LEG-06) | **G** |
| 오프라인·동기화 | 오프라인 솔로 무결, 아웃박스 동기화 성공률 ≥ 99.9%, 롤백/충돌(O8) UX 명세 구현 | **G**(NFR-R07) |
| 바이럴 | K-factor(초대 루프) 계측 파이프라인 가동, 초대→그룹대회 참가 퍼널 각 스텝 계측 | W(검증 목표 ≥ 0.3) |

**Q1 2027 — v1 파트2(수익화·다자산) Exit Criteria**

| 영역 | 종료 기준(측정가능) | 게이트 |
|---|---|---|
| 결제 무결성 | 서버 영수증 검증·엔타이틀먼트 위변조 차단 PASS, 구독 복원·환불·자동갱신 E2E PASS | **G**(NFR-SEC05) |
| 광고 안전 | 광고 SDK 초기화가 연령·동의 이후, 미성년/학생 금융·도박·코인 광고 차단 검증, 리필 가드레일(일 5%·회 $1~2k·쿨다운 4h·3회) 적용 | **G**(NFR-SEC07·§12.3) |
| 데이터 라이선스 | 크립토 실데이터는 거래소 재배포 약관 개별 법무검토 PASS 시에만, 미통과 시 시뮬 유지, `Quote.source/delayedMs` 배지 강제. 데이터 계약은 per-user 금지(§12.6 IC-6) | **G**(§11.3·§12.6·R2) |
| 프로바이더 폴백 | 실데이터 타임아웃→시뮬 폴백 < 5s(서킷브레이커) | **G**(NFR-R06) |
| IP2 파생 결정론 | 펀딩 Σ롱+Σ숏=0·옵션 만기(ITM/OTM·행사·배정)·arbitrage-free IV·레버리지ETF 경로의존 NAV property test PASS(NFR-C06) | **G** |
| 수익화 계측 | 인스톨→유료·Trial→Paid·광고 ARPU 재모델(§12.4·§16 O2) 대시보드 가동 | W(검증 목표) |

**Q2 2027 — v2(B2B·실데이터 심화) Exit Criteria**

| 영역 | 종료 기준(측정가능) | 게이트 |
|---|---|---|
| B2B 컴플라이언스 | DPA/FERPA·주별 학생 프라이버시 계약 선체결, 학생 시트 광고·결제·공개소셜 코드레벨 비활성 검증 | **G**(§12.5·§13.4) |
| 접근성 확장 | 라이트/고대비 테마 ≥ 1종, WCAG AA(EU=EAA 법적 게이트), 시맨틱 토큰 100% | **G**(NFR-A02/A04) |
| 실데이터 정직성 | "15분 지연·출처" 배지 100% 노출, 지연 데이터 실시간 오인 표기 0, 녹화 서버 틱 로그(해시체인) 진실원본 | **G**(§11.3·R2) |
| 확장성 | 대회 5,000명·리더보드 CCU 10k 부하테스트 SLO 유지, 원장 100k건 성능 통과 | W(NFR-S03/S04) |
| 레버리지 정합 | 강제청산·margin interest 대칭 차감, equity 음수 화해 규칙 **O13 확정(LEV-05+ACC-22, IP1에서 이미 해결)** 가 멀티마켓·크로스마진에서도 IM1~IM6 유지 | **G** |
| IP3/IP4 멀티마켓·코인 결정론 | 다통화 base 항등식 ±1최소단위·FX 삼각환산·DST 전환일 1시간 오차 0·크립토 24/7 앵커 콜드스타트 <100ms(NFR-C06) | **G** |
| 한국 출시 | 게임물 등급분류 필요 여부 법률검토 완료, 회피 시 한국 빌드 확률형·시즌패스·경쟁 리그 분기(O4/O12) | **G**(§13.4) |

**근거·설계 의도:** 각 단계 Exit Criteria는 §4.2 KPI(온보딩 70%)·§16 리스크·§18 O-이슈를 **릴리스 판정 항목으로 승격**해, "가설·부채·오픈이슈"가 미해결 상태로 다음 단계에 상속되는 것을 차단한다. G/W 분리로 "출시를 막는 것(정확성·안정성·보안·법규)"과 "추세로 관리할 것(K-factor·ARPU 등 검증대상 가설)"을 구분해, 게이트가 성장 실험을 과도하게 옥죄지 않게 한다.
